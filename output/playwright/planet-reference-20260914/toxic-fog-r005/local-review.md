# Toxic fog5 — native crossed alpha-card diagnostic

2026-09-14. Preserves rejected fog3/4 shells. Three gently bent native camera-independent cards per bank carry a density PNG with zero-alpha perimeter and nonuniform olive interior. One ordinary double-sided PBR material uses IOR1, roughness.98 and zero emission. No replacement shader, per-frame geometry rebuild or LOD topology change. Cards are intentionally open, not claimed manifold solids. Corner normals/UVs remain authored and explicit GLB IOR normalization preserves raw exports.

Twelve stable placement-owned banks total6,912 triangles. Same explicit anchor API as fog3; compositor r005 uses vertical scale.16 for plumes/.12 for low banks. Four focused NullEngine/identity/LOD tests, TypeScript and two Blender saved-density tests pass. Alpha decoded from PNG min0 max.458824 mean.072396; every perimeter pixel zero. Three-variant GLB/JSON audit has zero attribute/material/texture differences.

Context preview removes the nested membrane shells but still reveals a vertical card junction and bright fringe. The soft-bank art gate is not passed. Actual small-scale globe rendering may help assess alpha ordering and lighting, but no hardware, actual planet or independent visual acceptance is claimed. Root owns browser integration. No changes to reviewed Ocean/Temperate Cloud2.
