# Full-output fixed-detail qualification

The revision allowlist now covers the remaining fourteen invariant candidates: Rocky r010, Toxic r007, Crystal r013, Rocky moons2/1, Desert moons1/1, Gas moon2 revision1, Ocean moons1/2, Temperate moons2/2, and Toxic moons1/1. Exact revision strings are enumerated in `reference_full_output_fixed_detail.test.ts`. Unknown successor revisions and missing revisions remain unqualified for these layouts. Rocky moon revision2 uses `rocky-crater-geology`, the same composer family as Rocky r010.

The test invokes the actual `planet_reference_worker.ts` initialization and build handlers for seeds 38, 117 and 904 at LOD 0/1/2. It compares a SHA256 digest of every returned field except request ID and elapsed build time: positions, normals, UVs, indices, material batches, placement ranges, weather, smoke, and worker shadow envelope scalars. Toxic r007 uses the actual Toxic fog r005 kit and requires nonempty cleared fog output. The other thirteen candidates use nonvolcanic recipes without weather coverage or smoke. No weather or smoke behavior is forced to pass qualification.

A NullEngine cache test for each exact revision exercises distant approach, near view, retreat and abrupt Observe. All requests resolve to LOD 0, build one retained node, and dispose it cleanly. This establishes resource/identity behavior; it does not establish real GPU timing.

Desert r014, Ocean r007, Temperate r003 and Volcanic r023 remain genuine varying-detail main candidates. Volcanic includes LOD-dependent smoke and cannot qualify based on its body alone. Existing fixed-detail rules are unchanged except the explicit fourteen additions.

Validation: `NODE_OPTIONS=--max-old-space-size=4096 npx vitest run --config scripts/art_library/reference_full_output_fixed_detail.vitest.config.ts` passed all 15 tests in 96.85 seconds (one worker, no file parallelism). `NODE_OPTIONS=--max-old-space-size=4096 npx tsc -p scripts/art_library/reference_review.tsconfig.json --noEmit` passed. The test is registered in the full review suite and its applied TypeScript project. An initial run exposed two fixture metadata mistakes (rocky style spelling and Rocky moon layout); both were corrected before the complete passing run.

`broader-seed-capture-manifest.json` plans 56 serial jobs (28 candidates × seeds 117/904), each with close, alternate-angle and reference-scale views, exact kit hash, weather revision and preserved reviewed glow/local-light settings. It does not execute captures or claim acceptance. Shadow overrides from historical evidence are intentionally omitted so the current integrated correction is exercised. Desert r014 lacks a matching accepted camera JSON; Gas r005 has a fresh shadow capture request but no tiny view. Their planned 13.5 reference radius is explicitly provisional. Older Rocky r010 and Temperate r003 requests omit the radius field and use the capture tool 13.5 default.

The capture helper now resumes its actual Babylon render callbacks before every view. It requires three advancing frames with the effective requested LOD active, zero cache/worker pending builds and a ready scene, then freezes the screenshot. Each view records its frame/camera/LOD settlement. A failed build or timeout rejects capture. Two small Python harness tests cover changing and fixed LOD, pending work, shader readiness, worker failure, stalled frames and resuming a second view; both passed without a browser. Python compilation passed. Complete fixed-detail qualification and shared shadow inspection before executing the batch. Hardware timing, normal-game integration, public activation and owner artistic approval remain separate gates.
