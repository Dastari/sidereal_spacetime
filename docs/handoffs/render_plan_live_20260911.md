# Rendering performance release — 2026-09-11

The owner requested continuation and explicitly instructed that this agent's changes be made live. Public client `5a4c31c994c91bb6eb2fe667c2bee058dc3c666f0d6d975392d2b6b335012c75` is activated at https://sidereal.dastari.net/. Entry `/assets/index-DKNHMjt4.js` has SHA-256 `1fd91524f75b56f6a55f07afdc96e4dee83e2c3a93b68cc8a1f3800766790022`; public HTTPS bytes match the staged artifact exactly. This is publication, not completion of the rendering plan's outstanding measurements.

The isolated source candidate is `.runtime/release-checkouts/render-plan-live-20260911`, based on the exact source of previously live client `20b3025f`. Rendering-register commits were integrated with the already published camera framing, uniform bone TAA, character, backpack/drop and F3 controls. Source conflicts retained those published behaviors. The independently deployed Shipyard was not replaced with unfinished editor source. Unreferenced authoring-only role deltas in files absent from the public baseline were not used to introduce new editor modules. The retained R0 debug-overlay role patch is included in the live overlay implementation.

The public client includes role/capture diagnostics, local mapped glow batching, shader dust and scalar revisions, shared material variants and owned equipment/cargo pooling, local placement instances/structural maps, static caches and material invalidation, semantic shadows with the Lighting Off correction, limited body admission, and the experimental WebGPU backend with conservative FAST admission. WebGL and the existing AA default remain unchanged. R2/R4 input-identity conflicts, conservative FAST exclusions and all measurement limits remain as documented in the plan. Deferred R9 experiments and R11–R14 were not opened.

Validation: isolated `npm run check` passed 241 files / 1,389 tests and 77 document/provenance checks; full `npm run build` passed. The isolated baseline excludes unrelated construction tests, so this count is not comparable to the larger shared workspace's 1,523 tests. Existing bundle-size warnings remain. Source manifests verify that all 120 world-source files, 122 simulation files, 59 content files, 104 network files and 19,740 asset files match the previous live source before build. Compiled world output was validation only and was not published. The independently pinned GLB delivery configuration remains unchanged.

The initial ordinary source copy hit disk quota; only this attempt's incomplete candidate was removed. The successful isolated candidate uses hard links for unchanged asset/reference evidence, with independent editable source copies. Neither original assets nor previous releases were deleted or modified. Available disk space remains limited.

The database and development frontend were found stopped during release preparation. Added managed `python3 scripts/dev.py database-up`, which calls the existing database startup with publication disabled and never stops a running process. Its Python regression proves command dispatch does not publish or stop services. The existing database was started through that command; after it was running, the existing development frontend was restored with `up-client` without publication. No world module, schema or authority code was changed, no backup/reset/restore was performed, and the dashboard process remained running. This start-only recovery is separate from the guarded public-client activation.

Activation used the managed prebuilt stage and exact expected-live/staged guards. The public root, hashed entry and `/v1/ping` return HTTP 200. The normal public browser loads the exact new entry and reaches the real Dastari provider sign-in page. The review pane has no current provider session, so authenticated rendered-game and seated-Flight acceptance await sign-in; no authentication override was used. Prior development hardware captures do not substitute for that live acceptance.

Evidence is under `output/playwright/render-plan/live-20260911/`, with exact source overlays and private staging records under `.runtime/releases/render-plan-live-20260911/`. See `docs/rendering_performance_plan.md` for per-item evidence and remaining gates. Do not roll back the client to an older authority protocol; any correction must preserve the current live world and published inventory/character behavior.
