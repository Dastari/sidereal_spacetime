# Current integration ownership — 2026-09-09

This is a current coordination checkpoint, not a declaration that the construction roadmap or original goal is complete.

| Work | Owner | Current state / next gate |
| --- | --- | --- |
| Dedicated provider and public game | Root (verification completed) | Public domain cut over to the new project, exact PKCE callback installed, compressed-provider resource cache repaired. Recovery-fix release400dc9fdda68 installed; final interrupted-renewal, logout, fresh login and reload all passed; evidence is in public_login_verification.md. Login browser closed and GPU released. |
| Character bodies and equipment poses | External owner agent | Owner confirms implemented and live. Root preserves current paired assets and does not redesign or integrate competing pose changes. |
| Inventory UI | Same external agent, per latest owner message | Keep shared inventory UI ownership separate; root does not change these controls during auth/construction work. |
| Native multi-deck construction | Root + pressure_authority + pressure_native_render | Exact r006/a007 native room is integrated into an isolated server/client candidate: 44 native placements, persistent door/gasket/pressure state, actual collision and private views. Browser publish/two-spawn/reconnect/privacy checks passed; temporary provider role revoked and fresh self-grant denial verified. Full pressure, playable traversal, utility/cargo systems and complete Wayfarer template conversion are not claimed complete. |
| Latest cockpit | Construction integration | Pin existing R006 cockpit/polymer finish and later published roof/side armor. Preserve stable placements and equipment state; the1.25m opening still needs a qualified separate door/airlock interface. |
| Rendering performance | Root + debug_shadow_fix | Scalar shadow-cache comparison is installed in public build. Candidate F3 Lighting Off now suppresses all shadow submissions; browser render observers verified Deck/Flight. A discovered Max local lights reload-persistence gap is fixed with a default-storage regression; actual browser cap4/reload/reset proof passed. Full hardware Deck/Flight measurements, remaining optimization phases and broader lighting/material pipeline are outstanding. |
| Planets | Paused by owner | Preserve installed generation and next-pass specifications; do not restart visual iteration. |

Construction is the next main priority after public login acceptance. Native visual geometry, structural/collision/damage proxies and pressure interfaces remain separate contracts. Internal furniture and containers use entity health; structure/hull/armor and applicable external hardware retain the planned localized destructibility contract. Neither visual tiling nor a room label proves pressure sealing.

The public client release is immutable and independent of development and dashboard outputs. Source edits do not silently update it; publish deliberately through the managed lifecycle after validation. The world database was not reset or republished for public routing. Existing character, inventory and ship state must survive each later construction migration.

Scoped source checkpoint committed as `6de61c1c`; installed runtime/native artifact checkpoint committed as `7e0d5359`. Current bytes were preserved, not overwritten: 503 source hashes and 1,488 committed artifact hashes were verified, including 832 LFS pointers. Seventeen ignored review logs remain on disk and are explicitly excluded from the artifact manifest. The active ladder/traversal slice and broader historical art-library archive remain outside these checkpoints. `git gc` completed; the follow-up count is 21 loose objects / 171.50 KiB, 2 packs / 1.80 GiB. This local repository has no remote; these commits are not an off-host backup, deployment or final artistic sign-off.

## Current validation and immediate order

The qualified room is an opt-in review fixture in `sidereal-spacetime-dev-review-pressure-r006`, not a normal Wayfarer refit. The separate `pressure-r006-smoke` database passed multiplayer/authority smoke. Combined check/build/art validation passes after final formatting/import/storage fixes: 573 tests across 122 files. Native art qualification is not owner final artistic sign-off.

Native-room access cleanup and graphics preference browser checks are complete; the named browser is signed out and closed. Exact integrated record: `native_pressure_integration_checkpoint.json`. The next authoritative multi-deck traversal slice is now assigned: pressure_authority owns pure rules/content contracts and pressure_native_render owns a new preserved native ladder/shaft kit. Root will integrate world and renderer hooks after their interfaces are validated. Follow with accounted atmosphere supply/powered airlock behavior, cargo support and service routes, localized structural damage, and the complete semantic Wayfarer template. External character and inventory work remains preserved. Broad rendering appearance work and planets remain paused.

Known native-room limit: the whole conservative door sweep is reserved during movement, so closing can be blocked from much of the small receiving room. This is collision safety evidence, not acceptable final airlock usability. The review room starts in vacuum: browser evidence proves persistence and gating, not live charged gas transfer or powered life support.
