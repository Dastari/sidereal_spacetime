# Main integration coordination

The new integration_continuation agent owns combined R006 hull, r002 floor candidate, 73 cargo appearances, pose and login integration. Root owns the plastic rendering assessment. Native cargo batching preserves material AND matching vertex-channel sets; do not strip UV/tangent data to batch.

Latest construction checkpoint (`.runtime/roof-check.log`):103 test files /474 tests passed, typecheck and73 documentation checks passed; full isolated smoke, combined build and art validation passed. Eighteen Python checks and the lint/format ratchets also pass. Native wall/hinged-door and matching12-shape roof working candidates are integrated; the normal schema was published additively without seeding construction instances. See `construction_roof_integration.md` and the release records for exact artifacts and actual browser evidence. Canonical combat muzzle alignment, quick-click firing and native floor contact are fixed and actual-game verified. The specialist paired r002 is integrated only behind development URL `?poseReview=r002`; it is not a canonical rig replacement or final art approval.

Floor integration preserves old fitting volumes with local `PartPlacement.fittingProxy` offsets. Visual native floor origin is independent; do not restore the overlapping full-square proxy as installed wall fitting. The original proxy is clipped at the native 0.1875m deck top without mutating its source occupancy; seven assembly tests pass, including partial-cell contact, support snapping and the entire installed layout.

GPU review is serialized between root's IFCS, geometry and planetary agents. External recording/browser sessions also consume the software renderer; preserve their ownership and allow extra timing. Actual same-origin response pass-through may be used to bypass Chromium transport stalls; API state and asset bytes must remain real.


The external combat specialist delivered r002 in `combat_pose_integration.md`. Root applied the reviewed parent patch and completed real-game shots, energy consumption, weapon-switch and muzzle-ray checks. Optic/stock placement and acquisition timing remain specialist-reported limitations. See `combat_pose_parent_acceptance.md` for actual evidence and limits.

R006 authority collision revision 3 has passed isolated smoke and normal-client movement checks. The bow canopy, rear partitions and doorway are aligned; the forward-offset external capsule includes the longer nose while preserving mass, inertia, center of mass and aft extent. Root published only via the managed non-destructive path. IFCS has finished, restored the review actor/inventory and closed its browser.

Finish study04 is installed as a local candidate, with exact original R006 visuals retained. Native ice-r015 is installed and actual Observe review passed; reference fidelity remains incomplete. Native volcanic-r018 is now installed as a working candidate after root image review. Actual game Map → Cinder → Observe pointer selection, wheel zoom, right-drag orbit and Return passed, with character/ship/body/inventory rows byte-equal before and after. Evidence: `output/playwright/native-volcanic-r018-actual-observe.png` and its `-proof.json`. Earlier r001–r017 evidence remains historical; fine ledges, river shape and smoke still need refinement. Neither ice nor volcanic is final owner-approved art. No other planet family rebuild is part of this resumed pass; their detailed brief is recorded as next-in-line work.

Construction now has actual authoring PKCE/save/publish/reload and native floor/wall/door/roof proof. Managed additive schema is installed in the normal development database; no construction instances or grants were seeded there. Game review remains explicit `?constructionReview=1`. Two named-test instances were entered with independent door state, collision and resumable obstruction. A third roof-enabled instance passed native roof visibility and reload; all final Returns preserved inventory/appearance. Browser is closed. The pure pressure-layout adapter has30 focused tests but is not live pressure simulation. The NEW specialist is completing physical gasket/floor-seam contact and diagonal boundary interfaces; full pressure/deck traversal/cargo/services/damage and Wayfarer recreation remain incomplete. Planet/general rendering work is paused.
