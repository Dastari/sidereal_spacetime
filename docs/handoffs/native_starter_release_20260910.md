# Native personal Wayfarer release candidate — 2026-09-10

Status: exact world/client pair deployed after isolated ordinary-account browser and actual process-restart acceptance. Fresh public first-character creation, native rendering, interaction, reload and cleanup are accepted. The previous9388/a561 artifacts remain preserved as release history.

## Scope and source

- Trusted fixed-template starter factory and gameplay access: `ae2ec256`, `de464028` and `4b830790`. A genuinely new character receives a standing authored Wayfarer, four empty native cargo containers, a seven-item personal kit and functional qualified flight/interaction state. Gameplay ship name is `Wayfarer`; the exact reviewed document and source SHA remain unchanged.
- Private durable account entitlement and game-owned ship relation, separate from authoring grants. Existing characters keep their current ships, cargo and UUIDs. Native kit replay cannot seed old laboratory storage or uniforms.
- Native instance/deck/location views, walking/collision, cargo, sofa/grow-light, occupied native pilot controls and shared discovery use actual accepted gameplay scope. Root client checkpoint `0c793816` connects ordinary accepted native views/rendering without a query, suppresses legacy repair for owned starters, preserves review recovery separately and renders native effects/inspection. Its actual browser passed native pilot/TAB/thrust, cargo store/rotated-rifle/reload and process-restart continuity.
- Generic native authority/reconnect smoke: `837d739a`. The90-component laboratory armory remains a distinct legacy fixture; native smoke reports that limitation and tests denial, two body appearances and persistence instead of minting fake containers.
- Recovery compaction/helper: `e25abf4e`. Every historical recovery set remains preserved; only redundant raw representations may be removed after exact lossless verification.

No arbitrary template publication/refit, conversion of an existing player's ship, additional playable deck, crew invitation, mutable systems routing or staged external airlock is implied by this release. Existing-ship conversion is the next explicit migration task and must preserve current IDs and contents.

## Isolated authority evidence

Final generic smoke `sidereal-spacetime-dev-native-starter-r0006-smoke` and its same-name reconnect run passed on module SHA-256 `a962f39a07aeff0d3baae7fe80f13862b64fe22572428b71abe566a63046c2f0`. This includes the public-kit replay guard and clean gameplay name. The command named `smoke-restart` reconnects clients; it does not restart the standalone process.

Two ordinary Dastari accounts on `sidereal-spacetime-dev-review-starter-provider-20260910-smoke` passed actual SDK creation/replay, independent native ships, no authoring grants, private-base denials, normal walking, four-container store/retrieve/replay, cross-owner and legacy inventory bypass denials, sofa/light state changes, native occupied-pilot thrust, remote shared motion and exact inventory/location reconnect. That fixture initially usedfb79; it must be updated additively to the final candidate before final browser acceptance of the kit guard. Details and private evidence paths are in [the factory contract](wayfarer_starter_factory.md) and [the native smoke record](native_starter_smoke.md).

## Installed artifacts and actual release evidence

- World SHA-256: `a962f39a07aeff0d3baae7fe80f13862b64fe22572428b71abe566a63046c2f0`, published additively through `python3 scripts/dev.py publish`. Normal database identity remains `c2005c24147323197826efa22f24e30d2d99a93a5a5091949f2ad25fa2127572`. No reset or existing-ship replacement occurred.
- Client digest: `2f5c1bd48a34bcf149c73e5c69c62a10ca64e3d095228462e4fef644a7e61874`, immutable directory `20260910-071131-2f5c1bd48a34`. Public entry `/assets/index-Dw3BRGSE.js`, actual HTTPS byte SHA-256 `cb6e098765b60389753138873749c10cec0e9656df3c7007dd1360101a4974ce`. Managed staging preceded the world update, then exact client activation. NPM and dedicated provider routes were unchanged; dashboard release remained independent.
- Gates: `npm run check` passed1,145 tests in191 files plus76-document/provenance validation; combined build and art checks passed. Python passed53 standard,9art and4native geometry tests. One earlier aggregate run timed out the existing20-second heavy planet terrain test while other gates were running; a fully idle rerun passed without changing planet source. Generic native r0006 authority and same-name reconnect also passed.
- Fresh private whole-server archive `.runtime/recovery-20260910-071039.tar`, SHA-256 `b0cbfb74bcaff97ad11eff0f97ee91cc20ca966d5c47e29b6ce6f51898b63cf4`,32,223,088,640 bytes,8,581 members. Managed writer interruption was51.863seconds, followed by the same server/data/key restart without publication. A4GiB actual free-space guard remained enabled. This archive has not been separately restored; earlier archive restore proof is distinct.
- The isolated browser fixture was first updated to exacta962. Its additive SQL comparison preserved all actors, locations, access, receipts, items, containers and functional objects; only an already-coasting ship's position/tick changed. Root browser `module-update.json` exactly matched its saved state.
- Actual process-restart browser comparison passed `exact:true, changed:[]` across actor/access/visit, all five carried items and two stored cargo items, containers and appearance. Evidence: `output/playwright/starter-browser-review/restart-before.json`, `restart-after.json` and `restart-comparison.json`. The fixture has no custom appearance row, so this is preservation of its actual default appearance, not evidence of a newly persisted customization.
- Normal authoritative snapshots before backup, after backup and after publication preserved26characters/ships/stations,603items,165containers,17inventory-state rows,85hotbar rows,3appearance rows and6weapon rows. Native review snapshots preserved2actors/ships,14items,14containers including8native cargo roots,20fittings, both receipts/access relations and both visits. Only already-moving ship positions/headings/ticks advanced. Private records and installed artifact manifest are under `.runtime/releases/native-starter-20260910/`.

Root's final public test uses a newly provisioned ordinary provider account with no authoring/admin grants. Provisioning deliberately did not create a game actor through an API; first-character creation must occur through the actual public UI. Any resulting additional ship/character is an explicit acceptance action after the26-character preservation snapshot, not an upgrade loss. Actual public UI login returned through Dastari PKCE and began with zero game actors. Clicking Enter universe created actor `e2751a5a-fbff-426b-9f45-023c76c98dfb`, ship/instance `8645c1b3-ade1-4644-a4bd-6dbd0070d036`, deck `ecb544b2-2df7-4193-8121-ba2511b5952c`, seven personal items and three personal containers. The name is Wayfarer, its exact trusted template is `362f37217f63a44a470676f104c8368bd0973ca03f5e29eab190bc4d6df71340`, and shared admission was automatic. Actual native cockpit/floor/equipment rendered with no review controls (`fresh-native-starter.png`). Reload preserved the full stable actor/access/visit/inventory/container/appearance/admission snapshot exactly. E switched the reachable grow light off and on through real accepted interaction rows. The new account signed out and named browser was blanked/closed. Evidence: `output/playwright/native-public-review/{onboard,create,reload,interaction,finish}.json`. Observer hooks only exposed read-only scene/cache state; creation and interaction used actual UI/key input. Software-GPU stepping establishes visual/interaction evidence, not hardware FPS.

An old client-only rollback is not a complete rollback of newly created native authoritative state. Preserve the additive tables and IDs; use a forward-compatible corrective build or a specifically reviewed migration. A cold archive is a disaster-recovery artifact, not permission to discard later live writes.

The isolated starter browser also retrieved both stored items after the real process restart; all seven original personal item UUIDs returned and cargo became empty. `stored-after-process-restart.png` is the actual reopened inventory rendering. That account signed out and its named session closed.

Final public first-character acceptance passed through the real Dastari callback and normal Create/Enter UI at `/`, with no review query. New ordinary account actor `e2751a5a-fbff-426b-9f45-023c76c98dfb` owns ship `8645c1b3-ade1-4644-a4bd-6dbd0070d036`, seven personal items and three personal/nested containers. Exact reload preserved those identities and state. The native grow light toggled off/on and returned to its original enabled state. Evidence: `output/playwright/native-public-review`. This new account is an explicit acceptance fixture added after the26-legacy-actor continuity snapshot, not an unexplained count change.
