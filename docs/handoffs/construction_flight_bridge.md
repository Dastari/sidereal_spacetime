# Qualified authored instance to playable ship bridge

Status: registered additive flight/pilot/admission authority and generated bindings are implemented and passed an isolated real-provider journey. **Not published to the normal game database; browser acceptance is in progress under the parent.** Updated 2026-09-10. The prior cargo/seat/light release remains independently pinned.

The current qualified native Wayfarer can be spawned, walked through and populated with independent cargo/interaction state. Its review entry temporarily sets `character.shipId` to the construction instance UUID, but that instance has no actual `ship`, station, shared motion or flight-fitting rows. A room document alone cannot grant piloting.

## Implemented first bridge

`packages/sim/src/construction-flight.ts` validates the persisted instance by reconstructing the exact source through its UUID map and the existing native walking qualifier. It accepts only source SHA256 `362f37217f63a44a470676f104c8368bd0973ca03f5e29eab190bc4d6df71340`, instance revision1, and existing one-deck qualified geometry. A copied hash with altered geometry/mapping fails. The server supplies a bounded, reserved system-local berth; the reducer request carries no transform.

The plan uses the existing **instance UUID as ship UUID** so deck, cargo, interaction and actor-location joins remain consistent. It allocates eleven fresh UUIDs: one pilot station, one flight computer and nine actuators. Each device maps its instance placed-object UUID back to the exact source definition. The station maps `equipment-control-seat` and `equipment-control-console`; the nine engine/thruster source IDs come directly from `LAB_FLIGHT_ACTUATORS`. Duplicate/reserved identities reject before writes. No existing ship, actor, admission, item, container or document is modified.

The complete plan includes a static ship row, a lean shared motion row with indexed cells and authoritative sample tick, station, computer, actuator bindings and flight definition. The initial station is **unoccupied and non-operational**; installing data is not a control grant. Activation remains a separate validated transition after the hooks below are implemented.

`packages/world/src/construction-flight.ts` supplies `installConstructionFlight(repository, request)` with exact instance revision and operation identity. The current live game/ownership/workspace `instance.spawn` access check precedes receipt replay. A new operation cannot install a second ship on the same instance. A retry returns the recorded IDs with no allocation/writes. The adapter reserves a safe berth, validates the full plan, then inserts all records/receipt synchronously. Storage errors propagate to the enclosing SpacetimeDB transaction; catching partial failures and continuing is forbidden.

## Physical provenance

The first bridge deliberately reuses `LAB_FLIGHT_MASS`, `LAB_FLIGHT_PROFILE`, `LAB_FLIGHT_SPEED`, `LAB_FLIGHT_COMPUTER`, all nine `LAB_FLIGHT_ACTUATORS`, `LAB_HULL` and compatibility `STARTER` scalars. These are the existing development installation values, **not** approved mass/payload/armor derived from art. Dynamic cargo/crew/fuel mass and routed power/fuel availability remain unimplemented.

Native GLB origins and physical force points differ. For example the port main drive visual is at (-3.625,-11.5625,.125), whereas its authored thrust application remains (-3.6,-14) with the existing height1.375. The bridge preserves the authored force point, rotation and thrust; it does not silently move forces to asset origins. Future socket qualification may revise those points through a new explicit physical definition.

The reused capsule is the current planar contact representation, not a claim that detailed new armor/cockpit geometry is destructible or matched by a mesh collider. Existing structural voxel/proxy authority remains separate. Ordinary interior entities still require health/damage definitions rather than voxel deformation.

## Registration and activation contract

Private tables are declared in `construction-flight-tables.ts`; the actual typed database adapter is `construction-flight-authority.ts`. The per-ship resolver in `construction-flight-resolver.ts` admits exactly ten complete fitting rows, binds source IDs to fresh runtime IDs, applies installed/powered availability and fails closed on missing/corrupt/extra fittings, changed instance revision or flight-definition hash. The resolved fitting IDs have been exercised through the existing `stepSystemSpace`/IFCS solver: enabled authorized input accelerates, disabled input emits zero thrust and produces no idle motion change. These modules are now registered in source and the isolated flight review database.

A content hash of the complete flight definition is stored at installation so source-code/content changes cannot silently retune an existing ship. The actual adapter preserves owner identity privately in its receipt table but projects only the minimal repeatable installation result. Global TypeScript check passed in `.runtime/construction-flight-tsc.log`.

1. **Register the implemented private additive binding/fitting tables.** A ship-to-instance binding stores exact source SHA, instance/deck/revision, flight-definition revision and dormant/active state. A station binding stores the fresh station ID, actual deck/seat/console placement IDs. Fittings store fresh runtime ID, ship ID, sourceDeviceId, placed-object ID, immutable definition and operational state. Index by ship; cap at the exact ten devices in this first definition. Preserve the same placed UUIDs and cargo rows. Do not reuse static source IDs as runtime fitting IDs.
2. **Register the implemented ctx.db adapter.** The repository is bound to `ship`, `shipWorldMotion`, `station`, private binding/fittings/receipt; the mandatory server berth hook must be connected to the existing canonical allocator. Reuse the shared-system bounded admission budget/clearance algorithm and canonical-system seed validation. Execute publication/spawn and flight installation as clearly distinct owner actions, with explicit additive operation receipts. Preserve old world-motion snapshots and do not run live refit implicitly.
3. **Wire the implemented shared tick resolver.** `stepSharedWorld` currently hardcodes the LAB mass/hull/device IDs for every ship. Replace that with a server-side resolver: legacy stock ships retain the existing definition; a bound construction ship requires its exact current active definition and complete fittings. Invalid/missing binding must fail closed, never fall back to a stock ship. Feed the existing `stepSystemSpace`/IFCS/contact solver; do not duplicate physics. Output telemetry must use the fresh actuator UUID while retaining the placed-object mapping for the renderer. Continue once-per-system stepping and idle no-op behavior.
4. **Native deck station authority.** `useStation` currently checks only laboratory XY range. Construction pilots require actual instance/deck, current owner/crew access, station operational state, qualified approach/seat support and LOS, unique occupancy, no conflicting couch/stair/traversal state and active input lease. The seat's own collider is handled by a separately qualified seat envelope, not globally removed. Loss/disconnect recovers to a supported approach and zeros IFCS input. Recheck station grant at input consumption. Ownership alone is never a pilot grant.
5. **Boarding/admission.** A flight installer must not teleport the actor or replace `worldAdmission` as a side effect. Explicit boarding/selection transitions must validate both source exit and destination entry, preserve the original ship and clear old station/input state. Replace the current review-return shortcut only through a defined playable location contract. Multi-account crew entry remains a separate permissioned path; an inventory grant cannot create it.
6. **Owner/remote presentation.** Owner clients consume the actual instance document/placement bindings and fresh station/device rows. Remote views map only the approved public exterior package, never private construction documents/interiors. TAB remains user choice. Read-only discovery must not cause an instance to become a ship or grant control.

## Acceptance gates

Fifteen focused tests across three files pass in `.runtime/construction-flight-tests.log`: two native instances with22 distinct flight UUIDs, source reconstruction/tamper denial, exact existing force points/ratings, finite bounded samples, identity/revision/authority rejection, empty dormant station, zero-write replay, full synchronous write set and emulated transaction rollback. The emulator is not proof of actual DB rollback.

Before normal activation: register actual tables/adapter, spawn two fresh instances on a new isolated database, show distinct fitting/container/station IDs and unchanged original ships, then prove native seating grants control only to its occupant. Exercise shared motion, collisions and fresh actuator telemetry, station exit/disconnect/grant loss, pilot switching and reconnect persistence. Review the actual game with both instances. Source/asset revisions and all remaining physical limits must accompany the matched world/client release. No pure test or static review is final art/flight approval.

## Registered native pilot authority

`construction-pilot.ts` in sim qualifies the exact existing pilot seat collider and its source UUID map. The approved standing approach is (0,9.375), seated XY is the existing (0,10.25), and current supported elevation is .1875m. Normal walking retains the seat obstacle. A scoped seating transition excludes only its exact one native polygon and matching boundary edges; console, hull and all other obstacles remain. The full swept .3m body path must reach the seat. Exit searches eight bounded supported positions; other characters can block them.

`construction-pilot.ts` in world implements synchronous entry, command-consumption checks and recovery over a repository. `construction-pilot-authority.ts` binds that repository to actual private station/flight/location/grant rows, `constructionCollision`, native support and the existing `consumeInputControl` lease validator. User entry additionally requires that caller connection hold the lease; server command consumption validates the stored live owner lease. `construction-pilot-tables.ts` declares the occupied-seat reservation with unique station identity and a pending-recovery index. The table and reducer/tick hooks are registered in the isolated flight candidate.

Entry rechecks live game identity, exact accepted instance/deck, owner review permissions, operational active flight/computer, lease, station revision, standing/non-traversing pose, supported proximity and swept approach. It claims the station, records the private seat, updates accepted pose, clears prior input/aim and records an idempotent receipt. No ownership-only or click-only flight grant exists. Current command consumption rechecks exact occupancy/deck/revision/pose, current grants, active powered definition and current lease; a valid idle check writes nothing.

Disconnect/grant loss recovery does not require the capability that was lost. It clears motion input/aim and tries a supported, unoccupied exit. If all exits are blocked, the unique reservation remains pending, denies control and retries only through the pending index. Repeating the same blocked recovery produces no additional row writes. Invalid geometry is detected entirely before any writes and holds a pending reservation; errors from subsequent database mutations propagate normally to transaction rollback. A stale reservation cannot teleport an actor who has moved to another instance/deck.

Nine additional tests in `.runtime/construction-pilot-tests.log` cover actual native geometry, unmodified walking collision, entry/retry, wrong deck/revision/occupancy/permission/lease/support, current control checks, disconnect/grant-loss exit, blocked exit no-op retries, altered geometry, detached actor safety, and the **actual** input-control validator expiring a session and clearing stale motion. Full tsc passes. The ctx.db pilot wiring has now passed the actual registered provider seating, motion and recovery journey described below. Root owns actual client browser acceptance and matched publication. Native pilot entry and command consumption additionally require the actor’s current owned world admission to name this ship; entering a static review alone cannot grant flight control.

## Audited registration patch and migration gates

`python3 scripts/stage_construction_flight_patch.py` writes `construction_flight_integration.patch`, exact input hashes and a private in-memory preview. `npx tsx scripts/validate_construction_flight_patch.ts` typechecks that preview against the current repository without editing parent-owned files. The pre-application patch passed this in-memory gate, was explicitly released by the parent and has now been applied. The preserved patch/input hashes are historical integration evidence; do not blindly rerun the staging script against already-applied anchors. It touches only world/index, shared physics, exported existing berth selector, auth seat release, grant callbacks and construction-review exit. It must be regenerated/reviewed against the then-current source before application.

The patch registers six new private tables, explicit install/activate/pilot-enter/pilot-exit and saved flight-review switch/return reducers, and two keyed minimal owner status/fitting views. Activation validates the complete dormant definition and exact native seat geometry, then makes the station operational without assigning an occupant or moving anyone. The shared physics hook resolves actual per-ship definitions while preserving the existing common contact solver. Dormant ships retain their rigid physical definition with their computer disabled: they remain collision participants and do not stall another ship's movement. A regression exercises that case. Fresh actuator output IDs remain mapped to their actual placed objects.

Auth recovery runs **before** the legacy station clear. If every supported exit is blocked, the native reservation/occupant survives as pending and denies thrust; the legacy cleanup must not release its station behind the recovery adapter. Grant callbacks only target the affected owner's native pilot rows. The world tick retries the pending index. Only explicit `PilotGeometryError` from pure preflight is converted to a blocked recovery; unexpected errors and all write errors propagate. No subsystem catches partial writes and pretends it committed independently.

This is an additive migration: pre-existing ship, station, item, container, appearance and admission rows are not backfilled, renamed or reset. Existing stock ships have no construction binding and retain their stock definition. Existing static review instances remain static until an explicit installation operation. No login performs installation or transfers inventory. Generated bindings were regenerated after registration. Existing auth/construction test fixture databases received explicit empty pilot/flight sidecars; production code must not use missing-table fallbacks.

### Explicit preserved-state owner switch

An installation cannot change `character.shipId`, `constructionLocation` or `worldAdmission`. Those are separate transitions. For the first isolated pilot proof, keep existing owner-authorized review entry and add an explicit **begin flight review** action after the actor enters the exact instance. It must validate live identity, owner/grant, accepted visit/deck, current original admission/ship revision, empty source/destination stations, active target installation, same canonical system and no couch/stair/traversal reservation. Save the complete original admission snapshot in a private per-character review-transfer sidecar keyed to the current visit ID, then replace only that actor's admission ship ID with the already-spawned target. Never move/reset the original ship or its motion/contents. Clear previous input/aim and preserve item/appearance UUIDs. A receipt makes retries inert.

Returning from flight review restores the saved admission and original construction-review return pose only if the exact current visit/actor/target match. It remains available after authoring grant expiry, once native pilot recovery succeeds. Changed or missing original system/ship/admission must reject with a recoverable diagnostic, not fabricate a destination. Reconnect while reviewing retains the explicit transfer sidecar; reconnect after return retains the original admission and all same item IDs. This is a developer review transition, **not physical crew boarding**. A production owner switch must have its own clearly labelled action and validated entry contract; it must not hide in the installer or ordinary login.

### Implemented explicit switch/return and client projection contract

`packages/sim/src/construction-flight-admission.ts` implements the preserved-membership transition, and `packages/world/src/construction-flight-review.ts` binds it to actual `ctx.db` admission/location/receipt rows. The new private `constructionFlightReview` sidecar records only the original membership relation and current accepted visit; it never stores a ship transform or inventory snapshot to rewind. Begin requires a standing actor already inside the owner-authorized review, both stations empty, current active installation and exact visit/admission revisions. It changes only membership and clears input/aim. Return restores membership with a new monotonically increasing revision and invokes the existing accepted review return in the same reducer transaction. It remains available after authoring grant loss once the actor has safely left the pilot seat. Original ship motion may change while away and is preserved on return. Old begin receipts cannot reattach a completed visit.

Eight focused tests cover the pure admission/restore rules plus actual adapter mutations and the real existing `leaveReview` helper: reconnect with retained state; unchanged character/items/appearance and independent ship motion; wrong owner/grants/revisions/deck/source system; blocked return while piloting; idempotent requests; and a late receipt failure rolling back the entire combined transaction in the emulator. The subsequent isolated provider journey exercises the registered path; the emulator remains the only deliberate late-error rollback injection.

`construction-flight-views.ts` supplies two projections, wrapped by `auth.gameView` in the registered source:

- `own_authored_flights` / `ownAuthoredFlights`: `{ shipId (PK), stationId, stationRevision, deckId, lifecycle, revision, active, flightAdmitted, visitId, visitRevision, admissionRevision, seatState, seatRevision }`. Revisions are bigint. `stationRevision` is the flight binding revision used by pilot CAS. An unrelated/no current visit is encoded as empty visitId and visitRevision0. `seatState` is `none`, `seated` or `recovery-pending`; no raw recovery diagnostic or other occupant identity is exposed. `active` describes installation state, while `flightAdmitted` describes the exact current actor/visit/deck/membership relation. Neither is independently a capability grant. The minimal saved return relation remains visible after authoring grant loss, without exposing a workspace document.
- `own_authored_flight_fittings` / `ownAuthoredFlightFittings`: `{ id (PK, fresh device UUID), shipId, placedObjectId, sourceDeviceId, kind }`. Only the current actor’s exact accepted owned flight instance is returned. Ten complete unique computer/actuator rows are required, with a strict bound. The renderer maps fresh telemetry IDs through `sourceDeviceId` to the existing known effect mount; it does not infer thrust positions from GLB origins. Unknown or missing telemetry remains dark.

Five focused projection tests cover exact field allowlists/primary keys, active-versus-admitted distinction, owner/visit/deck/revision ambiguity rejection, safe pending recovery status, unique bounded device mappings and foreign-row withholding. The registered shared discovery guard replaces its blanket construction-visit exclusion with `hasAcceptedAuthoredFlight`; static visits remain excluded. Existing session/retired identity/system/cell/exact-distance guards remain. The guard permits the owned saved flight membership to remain visible for safe return even if the installation later becomes dormant; operational control is independently denied by the current flight/station/access/lease checks.

The client must opt into real ship motion only when its current accepted actor/location/admission/status agree. The current static review path historically zeros all ship motion and disables shared subscriptions. Root owns the corresponding presentation split and fresh-ID telemetry mapping. This does not activate physical crew boarding or silently convert ordinary login into a review transition.

### Fresh authored starter ships

For genuinely new characters, a trusted published starter-template entitlement can create the qualified construction instance, independent empty cargo/interaction state, independent flight fittings/station and one canonical shared berth before creating the actor at its qualified spawn. The one-time existing starter-kit receipt issues personal carried items once; ship containers begin empty. Select the exact server-pinned qualified template, never client JSON or whichever dashboard draft is newest.

Do not grant every player Shipyard `draft.write`, `instance.spawn` or administrator rights to make this work. The trusted starter-template factory should reuse the same pure construction/installation validators behind a narrow onboarding entitlement. Gameplay access to the owned occupied ship/deck must be separate from authoring-workspace permissions. Existing characters continue their current ships by default; no new kit, refit or appearance reset occurs on reconnect. The normal UI transition to authored ships therefore needs the gameplay location/read policy alongside the factory, rather than merely changing one model URL.

### Next isolated acceptance matrix

1. Publish the additive module on a fresh named database; verify all six raw tables deny ordinary subscription. Run existing authentication, multi-tab input, stairs, cargo and interaction tests with empty sidecars, then baseline smoke. Preserve the current public database and prior evidence fixtures.
2. Spawn two exact qualified native instances; install each once and retry. Confirm11 distinct fresh flight IDs per ship, independent pre-existing empty container IDs, no copied inventory, unchanged original ships/admissions and no automatic occupant. Wrong owner/grant/revision/source hashes and occupied IDs reject atomically.
3. Activate one explicitly. Prove the other stays dormant but still participates in physical contacts. Verify zero-thrust output/idle writes for the dormant ship and independent fresh telemetry IDs for the active one.
4. Enter the exact current review instance and explicitly switch review admission with a saved source snapshot. Walk to the native approach, enter pilot seat, then send real leased intent. Confirm common shared motion advances once, other players observe the ship exterior and original ship remains unchanged.
5. Reverse aim/movement inputs, stand, switch tabs/leases, expire/revoke permission, disconnect and reconnect. Each loss cuts thrust; a blocked exit remains reserved and does not produce20 identical writes/second. Restoring clear floor recovers. Ordinary walking never passes through the pilot seat.
6. Return to original ship/admission. Verify character, appearance, pistol/backpack/weapon energy and all nested cargo contents/revisions persist. Repeated receipts do not replay movement or issue new kit. A new account onboarding proof must have a separate template entitlement and must not acquire authoring privileges.
7. Exercise actual module/server restart when the managed shared-server slot is coordinated; reconnect alone and `smoke-restart` verification are not claims of process restart. Record installed hashes, generated bindings, matched client, browser captures and remaining physical/boarding limitations before public activation.

## Actual isolated acceptance, 2026-09-10

The managed fresh smoke command `python3 scripts/dev.py smoke --smoke-name authored-flight --fresh-smoke` created **sidereal-spacetime-dev-authored-flight-r0001-smoke** without overwriting prior fixtures or the public database. Existing generic authority/persistence smoke passed. Registered world build and generated bindings passed; aggregate `npm run check` passed1,090 tests/184files plus76document checks before root's subsequent client additions. The parent later reported the combined client check passing1,110tests/188files. Do not treat either as final browser or art approval.

`scripts/construction-flight-smoke.ts` provides import-only actual reducer journey helpers. With a real provider authorization-code/PKCE account, two exact published native instances were spawned: `1813f164-24c5-418e-b666-c6a10a2ea96c` and `bf7a3be5-cbfc-4e62-b0e3-c6322faf47ef`. Both passed separate replay-safe install and activation, explicit saved admission, native approach/seating at(0,10.25), positive shared motion and fresh-ID actuator telemetry, supported exit at(0,9.375), unseated thrust denial and original membership/inventory return. The22 station/device UUIDs are distinct. All six new raw authority tables denied ordinary SQL subscription. Original ships and carried item UUIDs/state remained unchanged. The first exploratory attempt expected unseated thrust to return zero instead of reject; the real reducer correctly rejected it. The helper was corrected to assert rejection, and the complete two-instance journey reran on fresh additional fixtures. Earlier partial fixtures were preserved, not deleted.

Evidence remains private under `.runtime/construction-flight-review/`: `fixture.json`, `journey.json`, `recovery-result.json`, `role-cleanup-proof.json`, and the generic smoke record under `.runtime/smoke-runs/sidereal-spacetime-dev-authored-flight-r0001-smoke/`. The recovery journey reconnected a seated moving actor using a new socket, retained the accepted visit/admission, verified supported recovery and zero thrust, reacquired a new input lease and seat, revoked `draft.read` during thrust, verified safe exit/zero thrust/reentry denial, and returned successfully with the lost grant. All appearance/inventory data stayed unchanged. The ordinary review grant was restored for browser review.

Temporary provider administrator authority was then revoked. A fresh provider token failed a new construction-grant command; both helper-owned provider sessions were explicitly logged out and private token files deleted. The actor is back on the original ship. Parent owns the real browser slot and the ordinary account fixture. **An actual server-process restart was not part of this helper run; generic reconnect/persistence smoke is not a process-restart claim.** Distinct-account observation subsequently passed as recorded below. Final normal-game activation, starter-template gameplay access, physical boarding, routed utilities and dynamic payload physics remain distinct work.

### Distinct ordinary account and actual browser pulse

A second ordinary Dastari account was admitted on the same isolated database. Initially its ship at(250,0) could not discover the reviewed ship more than2.4km away. The first helper incorrectly expected both authored ships to be visible; the failed assertion was inspected and reported to the parent, and no authority predicate was loosened. Read-only diagnostics confirmed a valid canonical admission, indexed nearby ship contacts and body discovery. The secondary then occupied its own stock pilot station, claimed its own input lease and navigated by normal reducer intent to within300m of the primary. No primary character, station, grant, lease, transform or database coordinate was modified by the observer.

While root piloted the primary through the actual game browser, the independent account recorded793 motion samples for `1813f164-24c5-418e-b666-c6a10a2ea96c`: start(250,2916.683676210713), velocity0; peak observed Y velocity3.60m/s; finish(250,2968.4187802502165), velocity exactly0 across repeated samples. Net replicated motion was51.7351040395m. Both ends retained the expected authoritative sample ticks. The public descriptor remained `stock-wayfarer-exterior:6cd837094b61bb6d1e69aefe4629cd9fa7e0f51db02fee80d203a824af756589`.

The secondary’s owned authored status/fitting views and private construction instance view stayed empty, `ownShips` contained only its own ship, all six raw flight tables denied SQL, and foreign installation/review entry commands were denied. No private interior/cargo grant was introduced for discovery. Evidence is `.runtime/construction-flight-review/secondary-result.json` and `remote-moving-result.json`, with earlier out-of-range evidence preserved separately. The secondary was braked before its final disconnect; its helper-owned provider session was logged out and token file removed. Root owns browser image/interaction acceptance; these server/observer records do not assert final visual sign-off.
