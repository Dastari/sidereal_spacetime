# Shipyard renderer layers and native selection

2026-09-10: implemented renderer adapter; separate dashboard candidate, not a game-client deployment or final combined editor acceptance.

`HullViewState.preview?: LayoutPreviewPolicy` and the eighth optional `createAssemblyLayoutPreview.update` argument share one policy. `LayoutPreviewPolicy` and `LayoutPreviewLayers` are exported through the existing render/layout-hull boundary. Layers are partial booleans: walls, floors, roof, objects, exteriorHull. Native category visibility remains an additional restriction. Wall/floor/roof each map to their structural layer; superstructure and engines map to exteriorHull; cargo/equipment/decoration map to objects. Systems `suppressEquipmentSolids` hides cargo, equipment and engine solids. Parent UI owns mode defaults/manual overrides and schematic footprint/name/port SVGs. Floors without a matched native asset obey the same floor layer; semantic floor documents are unchanged.

Selection uses the game's union-mask silhouette through editor-only mask proxies. These share the exact native geometry, copy each independent placement's world matrix and remain excluded from the visible camera and picking with layerMask0. This avoids per-instance selection aliasing without mutating shared native surfaces or game renderer code. Enabled visible native geometry occludes the mask normally. Hidden layers cannot retain selection rendering. Mask matrices follow drag-preview transforms before document commit; no authoritative transforms change. Deselection and viewport disposal release the mask, postprocess, materials, proxies and frame observer. The existing placement extent guide remains a placement aid, not selected mesh feedback.

Eight focused tests pass across layer policy, actual NullEngine shared-instance resource/material/geometry isolation and native semantic floors. They cover all eight native categories, independent masks, additional category restrictions, Systems suppression, mirrored/rotated/moving placement transforms, hidden selection cleanup and repeat disposal. The owned source passes TypeScript; the combined editor gate belongs to parent after its mode/theme/core integration finishes.

Actual GPU proof used the production createHullViewport adapter and two approved native Standard Medium cargo instances, on a route-fulfilled fixture at the existing managed dashboard origin5174. No account, database, draft or extra service was created. Actual canvas click switched the selected placed ID; screenshots show a cyan exterior silhouette with unchanged native surfaces in3D andTop. Objects-hidden and Systems suppression each produced0enabled placement roots and0selection passes; restoring objects produced2roots/1pass, bounding-box count0, GL error0. Screenshot paths: `output/playwright/shipyard-silhouette-review/{native-selected,top-selected,systems-solids-hidden}.png`, all visually reviewed. The fixture validates this renderer path; full Shipyard shell/mode-policy/Systems SVG acceptance remains the parent's combined browser gate.

Combined-browser follow-up: explicit Top now selects alpha+π/2 (north-up/east-right for world XY→renderX/-Z); unchanged named views preserve manual orbit through document updates. Three orientation tests verify native and projected SVG direction and explicit-switch-only reset. Native floors now also require the catalog's numeric revision formatted as the exact native rNNN revision, consistent with the gameplay construction renderer. The added revision-only tamper test preserves matching bytes/selector and rejects the substituted provenance. Structural status text covers walls/roofs/floors without calling all context floors. Eleven focused projection/floor tests and full TypeScript pass; parent retains combined-browser/gate ownership.
