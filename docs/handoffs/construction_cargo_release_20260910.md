# Construction cargo and interactions: coordinated candidate

2026-09-10 · deployed through the managed lifecycle; public artifact and durable-state checks passed, final fresh public browser verification passed.

This candidate combines qualified Wayfarer low-step standing height, independent native-instance cargo inventory and truthful liquid-state projections, qualified sofa seating and grow-light controls, the normal Dastari onboarding wording, and cargo object naming. It preserves the earlier shared-universe entry and original-provider session proof. The independent dashboard adds an editable pinned Wayfarer draft and complete native floor context in its preview paths.

The next authored-ship flight/pilot bridge and external-airlock controller remain unregistered staged work. Passing their pure tests does not install them. This release does not claim arbitrary playable ships, complete pressure/utility routing, stacking rules, final art approval or live refit.

## Candidate gates

- Full check: 1,023 tests in173 files, typecheck and76 documentation/provenance checks passed at the combined floor/cargo checkpoint.
- Full world build, generated bindings, independent client and dashboard builds passed. Art checks passed.
- Fresh isolated generic authority smoke passed on `sidereal-spacetime-dev-threshold-cargo-r0002-smoke`; its creation reserved a new named database and never reset an occupied fixture. Bundle SHA-256: `9388fee2f396b792e53710751b19727f002bccee096ed2c072c4943be7212dcd`.
- The later one-line cargo storage-name correction in `dfbe5e95` passed its focused checks and was included in a fresh client-only build. No world schema changed for that correction.
- Root's real-provider browser review passed native cargo selection, store/rotate/drag/reload/retrieve, sofa sit/stand and blocked walking while seated, and toggling one grow light while the other two retained their rows/materials. Evidence is under `output/playwright/cargo-browser-review/`.
- Dashboard Structure preview and saved-draft preservation passed real browser review; final Hull/Objects context review is recorded in [the template handoff](wayfarer_dashboard_template.md).

Private logs: `.runtime/threshold-cargo-combined-check.log`, `threshold-cargo-combined-build.log`, `threshold-cargo-combined-art.log`, `threshold-cargo-final-fresh-smoke.log`, and `threshold-cargo-final-client-build.log`.

## Fresh cold backup and actual restart persistence

After [disposing only the previously verified temporary restore copy](restore_review_20260910.md), the coordinator budgeted the fresh archive against38,354,550,784free bytes,30,587,465,730unique logical database bytes and a6,476,005,376byte reserve. The actor was parked and browser state captured before the managed writer pause.

`python3 scripts/dev.py backup-database` created private `.runtime/recovery-20260910-052604.tar`, SHA-256 `bd96ef5487bbee576e852dd0f4b3f73975e5b805b4bc26bbe2e45d40753a34cf`,30,609,141,760bytes and8,201members. Database interruption was46.87seconds. The managed server restarted without publication or reset; the whole standalone archive includes both the normal database and the isolated interaction-review database.

Normal database snapshots preserved all26 characters/stations,603items,165containers,17inventory states,85hotbar rows, three appearances, six weapon states and two shared admissions. The isolated `sidereal-spacetime-dev-wayfarer-interactions-20260910-smoke` snapshots preserved all792items and memberships,152containers/scopes, two construction instances, the occupied location, eight instance inventory bindings, eight interaction bindings and60interaction objects. No rows in these sets were added or deleted. Moving ships' position/heading/tick/cell values advanced between non-atomic snapshots; this is not a bit-identical simulation-time comparison.

The actual browser automatically reconnected after this process restart and retained its actor, visit/support, items, appearance and keyed light revisions/enabled states. Evidence: `output/playwright/cargo-browser-review/restart-before.json`, `restart-after.json`, `after-server-restart.png`. Sorted keys were compared rather than relying on subscription iteration order.

Private server evidence: `.runtime/releases/construction-cargo-20260910/normal-before-backup.json`, `normal-after-backup.json`, `interaction-review-before-backup.json`, `interaction-review-after-backup.json`, `after-backup-continuity.json`, and `backup-space-budget.json`. The archive is mode0600. This proves current process-restart persistence; the new archive has not itself been extracted and boot-tested. The earlier00:19 archive retains that separate actual restoration proof.

## Publication boundary

Stage the exact matched client after the final browser checks, then publish the additive world and activate that pinned client through managed lifecycle commands. Keep the dashboard independent. Recheck database identity, stable records, HTTPS entry bytes and fresh OIDC login after publication. A client-only rollback is not a substitute for a compatible authority migration; never restore an old archive over later player state merely to undo an application change.

## Installed artifacts and publication result

The owner explicitly authorized this combined deployment. The validated client was staged first, the world was published additively with `--delete-data=never`, and the exact staged client was activated. The first attempt stopped before mutation because a new unregistered flight view had a TypeScript narrowing error; its owner fixed that isolated error. The retry produced the unchanged tested bundle and completed successfully.

| Artifact | Exact installed pin |
| --- | --- |
| World bundle | `9388fee2f396b792e53710751b19727f002bccee096ed2c072c4943be7212dcd` |
| Client tree | `a561f80776db35587edfc28a3a760621eb6de6b6ef1babd78cf60ca41edce488` |
| Immutable client directory | `.runtime/public-client/releases/20260910-053758-a561f80776db` |
| Client entry | `/assets/index-NKgC0rNY.js` |
| Entry SHA-256 | `b4db04c00a46f5e09cb160fa200f97242f8691d2d141f1f19002d7ae013eadf9` |
| Normal database identity | `c2005c24147323197826efa22f24e30d2d99a93a5a5091949f2ad25fa2127572` |

Both loopback and public NPM root, callback and entry bytes match the immutable release. The public database ping and dedicated provider discovery return200. All four managed processes (database, public client, development client and dashboard) remain alive. Publication necessarily disconnected clients for changed authorized-view projections; refresh loads the matched new schema. There was no data reset.

The post-publication authoritative comparison preserved all603 inventory items,165 containers,26 characters and stations,17 inventory-state rows,85 hotbar rows, three appearance rows, six weapon rows and two shared admissions exactly. All26 ship identities and static fields remain; moving ship pose/tick fields advanced between snapshots. Private proof: `.runtime/releases/construction-cargo-20260910/installed-artifacts.json`, `normal-after-publication.json` and `after-publication-continuity.json`.

The editable template is already available through the independent managed dashboard at `https://sidereal.tail7a58a6.ts.net:8445/shipyard`; its current source is served on5174 and both routes were verified. Its independently built artifact SHA-256 is `c1f343ae650d5676c90f6a9937e099d9012366d4e6ddf8dd0841d1cad8aecf11`. No immutable dashboard release mechanism currently exists; this statement distinguishes its managed development deployment from the pinned public game release. Final Objects placement/reload proof and roof/floor visuals passed before deployment.

The preceding317c/66b5 artifacts and all cold archives are preserved. Recovery must keep application protocol compatibility and newer player state; the older client alone is incompatible with newly projected fields. The source freeze is released for the next isolated flight/airlock integration. Root's final public fresh-login/render review passed; no hardware frame-rate claim or new art sign-off is made.

## Final public browser acceptance

Root signed into the exacta561 public release with a fresh Dastari flow and obtained a fully rendered, ready game scene. Existing actor `689b973d-a007-42cc-9177-293eef7035d5`, original ship `d00e8875-2443-4e86-914a-42827aca9d2e`, the accepted shared admission and all81 visible item rows were retained. Evidence: `output/playwright/construction-public-review/state.json`, `warm.json`, `finish.json` and `private-map-ready.png`. The account was signed out and the named browser blanked and closed. This completes the current matched release acceptance; the next starter/flight/airlock work remains separate.
