# Staged native stair renderer

2026-09-10 update: **renderer index plumbing and exact runtime assets are now integrated in the working candidate**. `packages/render/src/index.ts` dispatches validated stair documents into `loadNativeStairConstruction`, consumes tagged accepted stair XYZ through the existing construction movement state, animates ordinary stair walking and avoids terminal sliding to the old source anchor. The three exact GLBs and manifest are installed under `assets/runtime/construction/stairs-r000-a003/`. No App/net/world-index edit or service deployment was made by this specialist; no browser/GPU slot was acquired. The earlier staged adapter is now connected when an authoritative stair instance and accepted state are supplied.

The adapter loads the complete exact a003 fixture from three GLBs, checks entire source SHA-256 and each of the60 source part IDs/selectors/transforms against the generated native audit, clones shared native geometry/materials into distinct placed identities, and preserves right-handed source XY → renderer X/-Z with source Z → renderer Y. It does not generate voxel substitutes, physical ratings, lights or collision from appearance. Existing equipment-lighting registration is used with an empty authored-light list.

## Shared wiring

1. Implemented at the `packages/render/src/index.ts` construction load branch using the new dispatcher (the shared `construction-instance.ts` remains untouched): dispatch an exact `document.stairRoom` before the generic floor/roof mapper. Obtain `nativeStairRoomInstallation(document, actualInstanceRevision, actualStairRevision)` and call `loadNativeStairScene(scene,parent,{instanceId,selectedDeckId,lowerDeckId,upperDeckId,visitId,installation:native.parts,sources:NATIVE_STAIR_ROOM_SOURCES})`. This loads all panels once; do not also run generic floor/roof loaders. Neither fixture revision numbers nor rendered frames are authority writes.
2. The returned object matches the fixture scene conventions: `placements`, `meshes`, `cameraFrame`, `walkingElevation`, `setDoors` no-op, `setView`, `applyAcceptedStair`, `setStair`, `dispose`. Camera frame is centre sourceXY `[4,5]`, half extent5 m. Expose the new accepted stair projection in shared SceneState without changing ladder behavior.
3. Each frame call `applyAcceptedStair(currentOccupiedDeckId, ownAcceptedStairStatus)`. Stopped and blocked active states still count as stair occupancy; source deck stays in place until the terminal location row changes. It returns accepted source XYZ only for matching instance, optional visit, recognized phase and finite values. For avatar/marker/camera placement apply `[x,z,-y]` after regular pose solving, just as the existing ladder path does. Do not interpolate from elapsed wall time, invent a ramp or snap the actor to a selected upper deck.
4. Supply the current visitId in both input and accepted status when wiring permits it, so stale prior visits cannot drive presentation. Current staged own stair projection needs its visit field added or mapped from the validated current construction visit; the view itself already filters exact visit ownership. Remount only when the instance/document changes, not on every deck/accepted row update.
5. Lower-interior and active stair views hide upper-floor/lower-roof slabs **except the actual upper exit landing floor at source `(4,2)`**. Both native flights, frame, intermediate landing and every guard remain visible. The outer stair enclosure alone cuts away in interior/transit view. On upper arrival, matching slabs reappear; exterior shows the enclosure. The actual four floor/roof omissions stay physically absent in every view. No placement transforms change during cutaway.
6. During workspace read/grant loss, keep only the server-authorized immutable own egress geometry context described in `native_stair_authority_integration.md`. This adapter's normal input is the full exact60-placement fixture; it does not itself create an authorization exception or minimal egress view. A retained full private document must not be substituted for the missing server authorization contract.
7. Deliberately install the three pinned native GLBs at `assets/runtime/construction/stairs-r000-a003/` and run managed app preparation/build only as the integration owner. The content URL declarations remain staged until this is done.
8. Remaining App/SceneState contract: supply `options.construction.visitId` at scene creation and set `SceneState.constructionTraversal = {kind: "stair", instanceId, visitId, sourceDeckId, phase, x, y, z}` from the server's own active stair projection. The kind tag distinguishes it from the existing ladder accepted state. Keep `constructionDeckId` from the actual location row, and clear active state only when the server does. No client position intent is added. Ladder frames continue through their existing handler. World must register/drive the staged stair authority and expose the accepted projection before this can display motion.
9. Shared review needs actual Shipyard spawn, both direction walks, stop/reverse, real stair/guard visibility, grant-loss safe egress and reload. Existing character model/rig owner retains that code; foot IK/gait is separate polish. NullEngine success is not a screenshot or visual sign-off.

## Tests

-15 renderer tests passed: actual complete GLB loading,60 objects/3 imports, authored shared material and matrix preservation,16+16 slab placement/omissions, both flights/guards and exit landing through cutaway, stale instance/visit/phase/XYZ rejection, changed native placement rejection, corrupted source bytes and resource cleanup.
- Focused render TypeScript passed (`.runtime/native-stair-render-typecheck.log`).
- Combined new document/authority/render tests: **51 passed across3 files** (`.runtime/stair-combined-tests.log`). Root's shared compile/spawn tests and prior13 pure walker tests are separate suites.
- No browser review, aggregate build, world schema smoke or service deployment is claimed by this slice. `npm run art:check` passed after installing the native assets; the dedicated installer `--check` verifies the new stair hashes because the general art checker does not yet discover this new manifest automatically. Focused index dependency TypeScript passed (`.runtime/native-stair-integrated-typecheck.log`).


## Installed runtime provenance

`python3 scripts/install_construction_stairs_runtime.py` installed precisely the three sources from the a003 delivery. `--check` verifies source/generated audit consistency, full GLB hashes, generated runtime manifest and the installer hash. The runtime manifest records exact source paths/hashes, audit SHA,60-placement fixture count and the owner's2026-09-10 integration/deployment authorization. It leaves final artistic sign-off null and servicePublication false. No preserved native source/delivery evidence was overwritten.

App preparation/client and dashboard build must copy these artifacts through the existing managed process before they are served from a release. This specialist did not mutate either prepared public app directory during the authentication release freeze.


## Root client/browser acceptance, 2026-09-10

The client now selects construction state by actor, instance and visit; an expired full document is replaced only by the minimum authorized egress projection. It does not retain the private document or substitute the stock ship. Ordinary keyboard movement reached the midlanding and upper deck, descended to the original lower deck, then repeated a partial climb while both workspace grants expired. All full instance/deck rows disappeared and the exact11-field immutable stair pin remained. Reload retained walk UUID and accepted supported position (3.004298,4.635,0.9375). Walking to the lower landing returned the same character to original ship coordinates(0,10.25) and cleared the visit. Evidence: output/playwright/stair-browser-review/{entry,midlanding,upper-deck,descent,minimum-egress}.png and {upper,descent,state,egress-frame,diagnose,safe-return,cleanup}.json.

Browser findings fixed: egress now uses construction lighting and elevation tracking, removes stock ship room labels/emitters/flight effects, and gives explicit WASD safe-exit guidance. Review teleport/door controls are disabled during active stair walking. The server's input-cache-only stopped-pose writes were also removed;20idle keepalives now produce zero pose writes in regression coverage. The named isolated stair database was updated through managed smoke without resetting the actor.

The software-GPU review warmed real renderer frames manually while requestAnimationFrame was paused. Screenshots prove actual meshes/materials/UI and accepted positions, not hardware FPS or animation cadence. An initial automation reload wait timed out during setup; the subsequent live read and rendered scene confirmed identical persisted walk/position, followed by successful ordinary-key safe exit. No normal Wayfarer refit or public stair deployment is claimed. Temporary provider administration was revoked, fresh denial verified, helper tokens removed; the browser was signed out, blanked and closed.
