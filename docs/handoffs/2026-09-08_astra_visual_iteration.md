# Astra visual and flight iteration

Status: Integrated flight, inventory, crew, object interactions, dust/zoom and bounded lighting acceptance passed; full planet-family fidelity and structural plastic finish remain open
Date: 2026-09-08

This handoff supersedes the counts and unfinished-task statements in `2026-09-08_voxel_canvas_resume.md` for this iteration. The earlier handoff remains historical evidence. The active project is `/root/sidereal_spacetime`; `/root/sidereal` is preserved reference only. Review game through `http://sidereal.tail7a58a6.ts.net:5173/`, dashboard through the same host on port 5174.

## Implemented in this iteration

Three concurrent Astra specialists were rotated through ship surfaces, crew/equipment, canvas navigation, authoritative IFCS, geometry/prop fidelity and procedural planets. Each specialist inspected assigned reference images. Per-image translations, rejected iterations and precise limits are recorded in [ship surfaces](../ship_surface_iteration.md), [crew/equipment](../crew_equipment_iteration.md), [canvas](../canvas_iteration.md), [geometry](../geometry_iteration.md), [hydroponics](../interior_prop_iteration.md), [planets](../planet_generator_iteration.md) and [IFCS](../ifcs_iteration.md).

The ship exporter now owns overlapping cells deterministically, welds surface junctions and applies small convex manufactured-edge bevels while retaining immutable voxel matter and placed IDs. The final ship has 295,738 triangles and 126 material primitives. Three hydroponics trays retain their exact existing footprints and IDs, with modeled pots, soils, grow frames, service parts and varied foliage. Six inspected source-versus-sample trials selected sampled manufactured parts with explicitly authored botanical surfaces. This is intentionally recorded in GLB metadata; foliage is not falsely described as a pure coarse voxel reconstruction. Source Blender files are preserved.

Renderer lighting now uses low ambient/reflection fill, an exterior key excluded from cabin geometry, warm/cool room practicals, small cyan accent spills, a bridge source, nearest-sampled exterior shadows and two nearest cabin shadow maps. Eight eligible lights per mesh is the enforced WebGL2 budget, including actor movement; a NullEngine regression covers all rooms and the shadow count. Cutaway does not expose cabin geometry to the exterior sun. This is authored art lighting, not simulated electrical utilities or full global illumination. The real `integrated-deck-review.png` capture shows both deck and player after integration; it does not establish the cause of the user's earlier missing-mesh screenshot.

Flight zoom is independently retained from deck zoom, defaults to a 55 m vertical half extent and reaches 650 m. Wheel zoom works in both views. Already authorized celestial destinations have a local Observe/Return camera, without relocating the body or changing simulation. The environment receives the final camera extent and viewport aspect. Procedural planets use ten parameterized families with bounded terrain, clouds, mountains, deposits/emission and rings; Genesis `/planets` is a local recipe authoring workspace. Travel dust uses replicated velocity and a bounded seeded world-cell field.

The authoritative fixture now uses controller/allocator output for three main engines, four lateral maneuver nozzles and two retros. Fresh zero input brakes linear and angular motion through actual opposing thrust. Expired input or lost station authorization clears all thrust and coasts. Nine private, owner-viewed telemetry rows drive corresponding stepped exhaust effects; disconnected subscriptions clear visible plumes. Installed components, power availability and mass are still authored fixture data, not a persistent utility/refit system.

## Verification and pending acceptance

The final isolated authority smoke passed, including braking, expiry, telemetry privacy, contact physics, walking/sprint and revision edits. Pure lighting and exhaust tests passed. Full check/build/art verification and restart evidence are recorded here after execution.

Later actual-browser reviews verified the integrated deck, crew, inventory windows, portraits, diagnostics and distributed destination observation. Remaining visual gates are identified below. CPU source renders and geometry budgets do not establish final visual matching or hardware performance.

M1/M2/M3/M4 remain open. This iteration does not deliver multiplayer crew admission, production authentication, live modular refits, fuel/power networks, planet landing or combat. Inventory authority was added in the subsequent extension below. The full target image remains the art direction; current features must be judged from actual browser captures rather than treated as pixel-matched completion.

## Owner follow-up: wall artifacts and layered temperate planet

The owner supplied another live wall-artifact screenshot after the first integrated review. Duplicate occupied-cell removal alone was not sufficient. A controlled same-angle comparison reproduced the large wall artifacts with alpha blending and removed them with opaque endpoints. Retained cutaway and roof materials now use opaque mode when fully visible. A separate prow shadow artifact is undergoing bias tuning; it should not be conflated with the confirmed wall alpha-sorting fault. See [wall depth review](wall_depth_review.md).

The owner explicitly supplied `reference/PLANET_RENDERING_DESIGN.md` for the next planet iteration, along with actual and intended temperate screenshots. Its first milestone is a layered temperate world: finer cube-sphere terrain, a distinct ocean, cliffs/biomes, radial small trees, independent flatter clouds, atmosphere and LOD. This supersedes the coarse-globe approach for that slice; the existing ten-family generator is still useful scaffolding, not fulfillment of the new visual specification. A separate Astra helper owns tree geometry while the planet specialist owns the layered terrain/runtime integration.

The owner's subsequent planet refinement explicitly expands the layered styling to every family in `reference/art/planets.png`, with composable effects such as temperate terrain plus volcanic regions. Bright localized emission, atmospheric rims, backlighting and blended fog/smoke are part of that request. Temperate remains the first comparison pass, not the final scope boundary.

Resize flicker: the root found and fixed HUD texture reallocation preceding a throttled early return. Resize now always repaints/uploads immediately, and engine buffer resize occurs in the same render callback as redraw. A rapid-resize regression passes; browser acceptance is still required.

## Persistent inventory and animation extension

The owner's later request added character animation and actual inventory work. The Astra animation pass added readable knee sleeves/hinges, planted-foot leg targets, phase-preserving eased gait/weapon/seat transitions and fixed a zero-delta first-frame bug that stopped newly requested animation groups. Canonical Blender/crew assets were regenerated, tested and independently browser-reviewed. See [crew visuals](../crew_visuals.md).

The normal development database now has additive PRIVATE inventory state, items, containers, hotbar and bounded operation receipts, exposed only through explicit actor/reach-filtered projections. A once-per-character starter kit includes an equipped 8×6 backpack, pistol, carbine, scanner, medkit, power cell and filled canister. Nearby storage adds a crate and other weapons; engineering has a capacity-only fuel reservoir. Multi-cell rotation/packing, atomic equipment swaps, nesting/cycle checks, payload/carry limits, revision CAS and retry handling are implemented. Containers and their contents retain UUIDs through moves. No liquid transfer, fuel utility consumption, stacking, trading or live refit is claimed. See [authority](../inventory_authority.md) and [UI](../inventory_ui_iteration.md).

App composition now maps the filtered inventory into the reusable canvas grid, equipment page and hotbar. Server equipment overrides cosmetic hand/back previews; distinct pistol-type tools retain distinct visual assets. A disconnected subscription clears the equipment projection. The starter kit is issued through its idempotent authorized reducer after an active character connects. Normal publication succeeded without resetting data. Isolated inventory smoke passed including failed-swap rollback, private/other-actor rejection, range loss, rotation, overlap/bounds/liquid/cycle rejection and durable replay guards. An actual managed down/up followed by `smoke:restart` passed with stable item IDs, hand equipment and inventory revision, as well as vessel/body persistence. An earlier attempt used an hours-old moving-asteroid fixture that had left sensor range; refreshing only the isolated smoke database and repeating the real restart resolved that stale-fixture failure.

Root widened Vitest discovery to include app tests so composition guards join full checks. The integrated build and art validation passed after publishing the authored equipment icons and wiring the planet's shared primary-light direction. Live inventory review passed starter-kit retention, actual pistol/carbine equip and swap, unequip mesh removal, hotbar activation, rotated pointer dragging, invalid placement rejection and compact scrolling. The owner's second UI pass replaced the initial combined board with independently movable character, inventory and container windows; its actual-browser acceptance passed, as detailed below.

Planet acceptance is governed by [the owner correction](../planet_visual_correction.md) and subsequent cloud refinements: rendered PNG texture comparisons against `reference/art/planets.png` must pass before advancing. The owner further clarified that cloud masses need thicker beveled voxel volume reaching toward the planet, not flat sheets; this supersedes earlier advice to minimize each cloud cell. The owner subsequently accepted the temperate comparison. Light-blue atmospheric rim and water-reflection refinements are implemented. Other planet families and the mixed world remain under their final texture comparison gate; temperate acceptance does not establish acceptance of every family.

## Current acceptance and remaining gates

The second UI pass now provides independently movable character, inventory and container windows, viewport clamping, focus order, clipped scrolling and strongly glowing selected controls. Actual browser review passed authoritative equipment swaps and compact presentation. Per-look authored Engineer and Scientist PNG previews switch visibly while the actual carbine hand and field-pack back cards remain unchanged. These are explicitly labeled preset previews, not live animated or recolored character snapshots. See [graphics UI](../graphics_ui.md) and `output/playwright/ui-final-engineer-diagnostics.png`, `ui-final-scientist-diagnostics.png`.

F3 works independently of actor availability, starts with “Renderer starting” while counters warm, and displays completed-frame metrics. The real client reported 441 draw calls and 175 active / 333 total meshes after completed frames; this verifies sampling, not target hardware performance. Instrumentation stops when closed; lifecycle and completed-frame regression tests pass. SwiftShader review paused continuous rendering and stepped presentation frames for captures, so displayed timing must not be presented as a performance benchmark.

Normal non-destructive publication and the distributed catalog browser gate passed. The client subscribed to 16 bodies and exposed all 12 destinations across three pages, including Ember Garden on the last page. Observe framed the actual mixed world 7.99 km away; Return restored the ship camera. Full subscribed character, ship and inventory-item rows compared identical before and after, including stable UUIDs and equipped carbine/backpack. Evidence is `output/playwright/ui-catalog-last-page.png`, `ui-catalog-observe-ember.png` and `ui-catalog-return-ship.png`; see [planet lab catalog](../planet_lab_catalog.md). This accepts navigation and authority preservation, not every planet family's final visual fidelity.

All ten crew looks were exported and reviewed in source and actual runtime views, preserving the animation rig and authoritative equipment sockets. The owner's missing helmet-back report was confirmed and fixed across enclosed helmets; front, side and rear proofs now show rear coverage. The subsequent recon glow occlusion probe passed: rear green lens ghosting is removed while front lenses retain glow. See [crew Astra iteration](../crew_astra_iteration.md), `output/playwright/crew-ten-runtime-front.png` and `crew-ten-runtime-coverage.png`. These are ten reproducible reference-directed looks, not a claim of exact pixel matching.

World-anchored dust parallax passed actual motion/projection review. The owner's latest top-down dust size reduction and smooth wheel-zoom refinement remain active and require their own acceptance; they must not inherit the earlier parallax result. See [dust iteration](../space_dust_iteration.md).

A structural-only plastic material pass is staged for controlled ship screenshots. It is NOT visually accepted and is not the canonical published ship result. External agents own equipment assets, including beds, seats and terminals; this pass must not modify those assets or overwrite their canonical outputs. Current remaining gates are the other planet-family/mixed texture comparisons, the latest dust/zoom changes and this staged structural material comparison. Do not mark a broad implementation-plan milestone complete from these bounded visual results.


## Owner-paused planet work — 2026-09-08

The owner explicitly paused further planet implementation and is happy to leave the current planets as-is for now. Functional ten-family coverage, composable recipes and actual in-game observation remain implemented; this does not accept the outstanding art fidelity. Temperate retains its earlier acceptance. Significant gaps remain elsewhere, especially ice (missing macro cyan pillars/cavities and snow contrast) and volcanic (missing connected bright molten networks with near-white cores, strong plateaus/craters and smoke). Uniform small tiles still dominate both actual surfaces. See the [owner pause and latest comparison evidence](../planet_generator_iteration.md#owner-pause--2026-09-08) before any future owner-requested planet iteration. Keep this work separate from externally owned equipment assets. No further planet implementation is pending in the current pass.

The [planet lighting follow-up](../planet_lighting_followup.md) records the latest ordered lava spill, voxel AO/contact, primary sun/self-shadow, IBL, HDR lava/bloom, Fresnel, exposure and terrain priorities, plus reflective transparent ice and cobalt volcanic cliffs. It is planned only. Root is awaiting clarification on whether to resume the first three priorities or leave the whole list paused; do not start implementation from this handoff.

Subsequent owner clarification explicitly resumed planet lighting priorities 1–3 and separately authorized the bounded ice optical/material pass. The earlier pause still applies to macro terrain redesign and other backlog priorities. See the latest [bounded resumption](../planet_generator_iteration.md#bounded-resumption-lava-spill-ao-primary-shadows-and-ice-optics) for implementation versus pending visual verification; do not infer full family art acceptance.


The bounded planet lighting pass now has actual runtime evidence: volcanic spill on/off PNGs show adjacent cliff illumination; ice optical attributes/materials compile, with snow and exposed ice separated. The controlled shadow sweep selects `.003R` normal bias for ice only, preserving `.0008R` elsewhere and retaining sun/contact shadows. Final evidence is `ice-bias-0.003.png` plus `volcanic-lighting-spill-on.png` / `-off.png`. This is functional lighting/material acceptance, not full ice/volcanic art fidelity; macro terrain, molten-network structure, transmission and other next-pass work remain open. Planet browser is blank and no further expansion is part of this pass.


## Final integration: dust, light occlusion, performance and objects

This section supersedes earlier pending statements for dust/zoom, bounded planet lighting and cabin shadows. The broader all-family art direction is recorded as next-in-line work in [planet next passes](../planet_next_passes.md); it is not a claim of completed macro terrain or optical fidelity. Equipment Blender models, approved exports and native equipment hooks remain externally owned. See [active ownership](../active_agent_ownership.md).

Top-down dust now has reduced layer sizes and a three-pixel projected size cap while the 3D presentation remains unchanged. Actual wheel review showed continuous easing from radius 215.397 through 225.988, 256.226 and 270.915 toward 273.824, without a wheel-event jump. Bounded world-anchored depth layers retain parallax. See [dust evidence](../space_dust_iteration.md).

Cabin light leakage was traced to missing casters and shadow depth inherited from the huge camera range. Room and bridge shadow bounds now match their local light ranges, with independent opaque structural occluders retained through cutaway and visible ship geometry blocking hidden glow. Seven local maps remain available, but static maps cache and only nearby actor maps refresh each frame. Conservative caster culling plus caching reduced the controlled full-ship sample from 2,255 draw calls / 13,405,848 indices to 876 / 5,347,989. Three actual update/render frames repeated the result. A translated/rotated ship matched forced shadow refresh pixel-for-pixel. These are render-work measurements, not a hardware FPS claim; see [light and performance proof](../ship_lighting_occlusion.md). Exterior trim shadow aliasing and the unaccepted structural plastic finish remain open.

Proximity interactions are now published additively and integrated with actual ray selection, placement outlines and independent movable details windows using approved equipment thumbnails. Sofa seating, independent hydroponics light controls and existing helm actions use server validation. Medical beds and other equipment are inspectable without invented gameplay stats/actions. Live browser acceptance passed selection, range gating, sofa sit/stand and movement inhibition, per-tray light off/on, and movable windows. Entering the pilot seat preserved the selected camera; W/S operated flight in Deck view and Tab changed the view without releasing control. See [interaction contract](../object_interactions.md) and [UI browser evidence](../object_details_ui_iteration.md).

Final integration check passed 145 tests across 43 files with 47 project documents validated. Isolated authoritative interaction smoke and normal additive world publication passed without resetting the development database. Final `npm run build` and `npm run art:check` passed; `git diff --check` is clean. Build retains the existing large-bundle advisory. This bounded slice does not complete a broad gameplay milestone; doors, healing, utility networks and final concept-level planet/ship fidelity remain open.
