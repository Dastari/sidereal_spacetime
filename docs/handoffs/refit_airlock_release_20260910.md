# Conserved refit, native airlock and loading release

Status: publicly activated under the owner's deployment authorization. Current world is `2ade8d75` with client `f9e32399`. Exact HTTP/module hashes and durable records passed verification; final public browser review is underway. Prior `a962f39a` / `2f5c1bd4` remains retained recovery history.

The release adds explicit, revisioned existing-ship conversion to the qualified native Wayfarer; preserved item/container/station/character/ship identities and liquid capacity; a separately qualified fuel attachment and manual canister transfer; native airlock authority and return-origin preservation; and the client loading barrier. It does not automatically refit accounts at login. Cargo carrier stacking, closure art, planetary work and wider rendering optimization remain separate, unregistered work.

Refit authority is checkpoint eab3a3bd. Native airlock authority is f9731b30; its paired browser controller passed a full cycle and native-home return. The final isolated client source is f29ff4a7 plus correction d1d716d1 and data-only aada6bcc. The installed world artifact is2ade8d75a75f75e5d555c8f2c3a666c69c736300dff13611dd7964ea6984ac8c; its earlier isolated acceptance preceded the public activation recorded below.

Earlier working-tree validation: full combined check1,182tests/199files plus76docs; full build; art validation; Python tests; fresh generic authority smoke; real legacy317c additive upgrade and hidden inventory conservation; native mounted tank browser picking and1L roundtrip; nine-output telemetry conversion and conserved-station pilot/thrust/release. The later exact isolated candidate gates and loading/retry acceptance are recorded below. Detailed migration evidence is in [the refit contract](wayfarer_existing_ship_refit.md).

## Recovery capacity preparation

Before the next cold archive, read-only Proxmox inspection verified CT107 hostname sidereal and rootfs nvme_pool:subvol-107-disk-0 matched the active container. Its128GiB quota left7.6GiB free, insufficient to safely retain another complete archive. The underlying nvme_pool had approximately2.4TiB available. With explicit parent coordination under the owner's infrastructure authorization, `pct resize 107 rootfs +64G` increased only that quota to192GiB. The running filesystem then had72GiB free. No service/database restart, data deletion, archive replacement or account mutation occurred. Managed database/client/dashboard/public-client processes remained running.

All previous recovery archives and their manifests remain intact. The next managed cold backup was coordinated after the browser was idle and its before-snapshot recorded. Hashes, interruption duration, exact durable-state comparison and restore limitations are recorded below. No new restore proof is claimed from a quota increase or an archive listing.

## Activation order and recovery

Pin the final tested world bundle and independent immutable client build. Snapshot stable authoritative normal-world and isolated reviewer records privately. Take `dev.py backup-database`, compare state after the automatic managed restart, then publish the additive world with deletion forbidden and activate the matching immutable client. Preserve dashboard independence. Verify normal database identity, routes, artifact hashes and actual public login/gameplay. If activation fails, retain the source/artifact and use the recorded compatible prior client only when its view schema remains compatible; do not downgrade schema or restore over live data incidentally.


The final telemetry-adjusted module is retained privately at `.runtime/releases/refit-airlock-20260910/world-2ade8d75.js`; `candidate.json` records its exact hash and capacity preparation. This was the retained staging artifact before activation. The secondary normal-UI fixture was converted under the earlierbd018 module; later telemetry reconciliation was independently proven on a fresh real legacy upgrade, not falsely attributed to that earlier conversion. No production converted account exists before this release, so historical review projections do not require a live data repair.


Static rollback compatibility check: comparing generated bindings from the deployed native-starter source checkpoint0c793816 to the staged refit checkpoint shows no modifications to existing table/view projection files, including own construction location, authored flights and game-owned access. Changes add the native-airlock/refit view files and refit/liquid reducers plus aggregate registrations. The previous immutable client2f5c is therefore a schema-compatible fallback candidate for the additive module; its old interface would not expose the new refit/fuel controls. This is source-contract evidence, not a claim of a completed old-client/new-module browser test. Prefer forward client correction; never downgrade or reset world data as part of client rollback.

## Exact source candidate and isolated gates

Concurrent external character/cursor renderer edits remain outside this release. The immutable candidate is root checkpoint `f29ff4a7` plus data-only `aada6bcc`, which supplies two approved cargo GLBs required by an existing test. The detached checkout records that pair as `dbb96f33`. Test-only inert-keyboard regression `34478c00` is already an ancestor. Node workspace dependencies resolve inside this checkout, so live workspace HMR and package edits cannot enter its build.

The exact candidate passed `npm run check`: 1,160 tests across 200 suites and 35 linked project documents. Full `npm run build` reproduced world SHA `2ade8d75a75f75e5d555c8f2c3a666c69c736300dff13611dd7964ea6984ac8c`. Python passed 50 standard, 6 art and 12 native geometry tests. `art:check` passed all installed asset/provenance checks. These counts describe the isolated candidate; earlier larger working-tree counts included unrelated untracked tests.

The clean checkout exposed historical untracked validation inputs. Exactly 361 existing linked documents, editable sources, art evidence files and art-validator dependencies were copied into isolated test support, with every path, byte count and SHA recorded in private `.runtime/releases/refit-airlock-20260910/validation-support.json`. No app, package or runtime-asset file was replaced by this overlay. Tests and manifests still validate the actual sources and hashes; no check was skipped. This is reproducible with that retained support manifest, but is not a claim that all older art-library history was already committed.

Production client SHA is `d2fe10de7f3cf24cde1ec3407e7d6de98d24fad58c7f667dfcf66e5141223191`, entry `/assets/index-D2BKZcMx.js` (SHA `d76654dd03f1ea0fb3154c9a0e42b54ec404f5a08b8c22064bafa993f9b45b96`). It uses the normal database and public Dastari game origin, with no source maps. It is staged through the managed prebuilt-artifact path, not activated.

The private immutable fixture uses the same source pair, the existing refit-upgrade review database and HTTPS review origin 8444. Its tree SHA is `eff16702656ff20c520cef3be58ba80125538028379227a35be148cbcaaf2150`; private source maps are included for this review only. Root serves these exact files through its named browser's request routing; no extra service or production workaround is installed. Browser context local-network permission was needed because Playwright-fulfilled documents were classified differently from the actual served page. Success, delayed-load, error/retry and final visible-frame acceptance remain pending for this exact artifact.

Managed client tooling checkpoints `07d0ac62` and `590bd3fb` add verified prebuilt staging and expected live/staged digest guards under a release lock. Twenty focused lifecycle/CLI tests pass, including tampered bytes, concurrent release mismatch, no service stop on rejection and exact successful promotion. Immediately before publication, re-read current module bytes from private `st_module` and compare their SHA, plus the actual public artifact. The initial-program API field is historical and cannot substitute for this check. At that pre-activation read, public world remained `a962f39a` and client `2f5c1bd4`; no public mutation had occurred yet.

## Final correction, activation and recovery evidence

The final client adds only root correction `d1d716d1` onto the isolated pair above (checkout `a265df28`): stop rendering behind the initial asset-failure cover and avoid duplicating its message into an unrelated persistent action-error toast. Corrected `npm run check` again passed 1,160 tests/200 suites and35 documents, and both independently configured client builds passed. World/art/geometry inputs are unchanged. Exact retained world `2ade8d75` also passed the full generic smoke on newly created `sidereal-spacetime-dev-review-refit-candidate-final-smoke`, including native flight, collision, IFCS, cargo, interactions, combat, privacy and two-account reconnect.

The corrected immutable fixture passed actual initial load, deliberately failed asset cover, real Retry button navigation, full scene readiness and visible-frame review: 3,209 meshes, interior camera beta0.9553166, inert disabled only after readiness, no stale action error. Its SHA is `2aa4040804099f220953521474809a93796b27aff4d8114833d0ad7dffe567a4`, entry `/assets/index-9OO8MYiW.js`. Natural token replacement with rendering paused retained the same scene and unchanged106 GLB request count. Sustained software-GPU stalls can consume the token-renewal lead; the earlier transient view revocation then occurred at actual signed JWT expiry. Authority correctly revoked access and kept durable state unchanged. No authentication grace, retained revoked private geometry or hardware-performance claim was introduced. Root evidence is recorded in [the loading-screen contract](loading_screen_20260910.md).

The fresh private archive is `.runtime/recovery-20260910-113317.tar`, SHA `2f71b8f031837b25a304da5b51e1799e098154f62ec645269242e4fc091bcae5`, 34,458,419,200 bytes and9,358 members. The managed database interruption was32.161 seconds. The same standalone restarted without publication during backup. All previous archives remain intact; this new archive was inventoried/hashed, not independently restored. The earlier isolated restoration proof remains a separate recovery test.

Final before/after snapshots in private `.runtime/releases/refit-airlock-20260910/` prove the review database preserved all30 captured tables exactly, including both characters'198 items,36 containers, visits/access, appearance, hotbars and liquid balances. Live snapshots preserved all27 character/ship/station identities,610 complete item rows,172 containers, appearance, membership and native-instance state. Only ships already moving advanced position, heading and tick between snapshots; their velocities and identities were unchanged. Additive publication preserved the same records again.

Activation used the expected `2f5c` live and exact staged client guards after re-reading actual live module bytes and public HTTPS entry. Installed world SHA is `2ade8d75a75f75e5d555c8f2c3a666c69c736300dff13611dd7964ea6984ac8c`; client SHA is `f9e323997ab7319a1ec52b76cc703eb40c7d8813119977432c64d272a0d0ec67`, entry `/assets/index-DoFJkM_-.js` (SHA `a0979dfc3405befb5c0ce827d16cbf8dff93b1c811ff46e5c8c6dea998821b5f`). Actual module bytes, full client tree, HTTPS entry and unchanged database identity `c2005c24147323197826efa22f24e30d2d99a93a5a5091949f2ad25fa2127572` were verified after activation. All four managed services run; dashboard lifecycle/build remains independent. Final fresh public browser acceptance is pending root's result.
