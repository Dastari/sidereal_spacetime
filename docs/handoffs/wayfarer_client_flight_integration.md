# Authored Wayfarer flight: client integration

2026-09-10 — source integrated; isolated provider, review flight and normal game-owned starter browser paths accepted. Public cargo release remains separately pinned.

`construction-flight-presentation.ts` joins the accepted character, instance/deck/visit revision, owned flight status and admission revision before using ship world motion. Missing/revoked documents, static reviews, stale membership and non-finite samples stay stationary. Shared subscriptions also connect in construction review; the server admits only the exact accepted flight transition. This does not grant access by camera mode.

The review panel exposes separate install, activate and begin-flight actions. Returning restores saved membership through the dedicated reducer. Native E/helm actions use the fresh station identity/revision and native enter/leave reducers. A pending pilot exit retains the seated presentation and blocks returning until supported recovery. TAB remains a player-selected camera change.

Fresh actuator telemetry maps through the complete accepted nine-device projection to existing authored effect mount names; accepted UUIDs remain unchanged. Unknown/incomplete/duplicate bindings leave effects dark. Selection resolves new placed engine UUIDs to source stats and telemetry. The renderer previously used an empty exhaust implementation for every construction scene; the qualified-source display flag now creates the known exhaust geometry, still driven exclusively by accepted output. Other construction/egress fixtures retain no exhaust geometry.

## Actual evidence

On isolated `sidereal-spacetime-dev-authored-flight-r0001-smoke`, ordinary actor `378d2196-069e-4ea9-8da6-eaa2d8270f9b` entered instance `1813f164-24c5-418e-b666-c6a10a2ea96c`, selected Begin flight review and walked through the native cockpit threshold to the pilot approach. E accepted the seat at `(0,10.25)`; releasing intent braked the previously coasting ship to rest. Actual W intent changed accepted world motion. A separate ordinary account legitimately flew into discovery range and recorded793 samples including51.735104m displacement and braking to zero, with no private instance/fitting visibility. That remote evidence is in `.runtime/construction-flight-review/remote-moving-result.json` and `secondary-result.json`.

The initial visual check found the construction exhaust stub; after the fix, actual accepted positive throttle enabled the corresponding three main and two maneuvering exhaust nodes. `output/playwright/flight-browser-review/visual-flight.json` records accepted UUID telemetry and rendered effect names. Its historical `topDown` property is mislabeled: the camera began at beta0.015 and the explicit TAB changed it to the interior angle. This is a camera-choice check, not an automatic seating view switch. `exterior-flight.json` records the subsequent correctly named afterTab sample.

Software-GPU rendering was stepped for screenshots, so no hardware FPS claim is made. One assertion sampled zero throttle after the first long rendered pulse; the independent observer still saw movement/braking. The bounded diagnostic captured positive telemetry before drawing and exposed the actual missing-effect implementation. Development hot reload also briefly emptied the observed cache and recovered the pilot to supported standing; that harness interruption is not claimed as a persistence failure or a process-restart test.

Focused client tests cover stale admission/read denial, bounded telemetry mapping and native engine inspection. Full check passed1110 tests/188 files before the final effect gate and normal starter additions; aggregate gates must be rerun for the next combined candidate. Normal owned-ship access, permanent location views and prevention of legacy lab reseeding are now connected; final aggregate/restart/public gates remain below. This review does not implement physical crew boarding, routed fuel/power, arbitrary edited-ship qualification or final art approval.

Actual return restored original ship `d59c5483-a0ff-4cdd-a5fa-fa46ce1b0588` and retained every carried item UUID; `exit.json` records the comparison. The account signed out and the named browser was blanked/closed. Exterior screenshot is actual top-down rendering, with the lower engines partly obscured by the HUD; enabled exhaust nodes and accepted telemetry are recorded separately.

## Ordinary game-owned starter browser

The normal client subscribes to keyed, server-filtered construction location/instance/deck/flight views plus `ownGameShipAccess` without a query flag. Authoring grants remain a review-only subscription. Accepted permanent gameplay membership suppresses legacy catalog/kit repair and hides temporary review controls even if a review query is later supplied. Geometry waits for ready subscriptions and the current accepted document; the client never creates an authority binding. The HUD uses the gameplay ship name for an owned instance. The exact template document/hash remain unchanged.

Actual ordinary Dastari login returned to `https://sidereal.tail7a58a6.ts.net:8444/` on isolated `sidereal-spacetime-dev-review-starter-provider-20260910-smoke`. Actor `819e9895-b656-481f-afa4-5635e4a55c0d` retained ship/instance `357bf50f-943a-4093-b2a3-9bece02008f9`, seven personal items and three containers. The native R006 cockpit, floor kit, equipment and roof rendered with no walking-review panel. E acquired the independently allocated native station without switching the chosen camera; accepted W thrust drove fresh UUID telemetry and the corresponding exhaust nodes. TAB explicitly switched exterior/interior views, and E exited onto the supported approach. Evidence: `output/playwright/starter-browser-review/{warm,pilot}.json` and `native-starter-interior.png`, `native-starter-exterior.png`.

Actual keyboard walking reached native storage. A first surface-pick attempt was correctly occluded from the initial camera; ordinary right-drag orbit exposed the container. The inspector opened its real inventory, the pistol transferred by shift-click and the rifle by drag with R rotation. Reload preserved actor/visit, appearance and both complete stored-item rows exactly. Evidence: `cargo-walk.json`, `pick.json`, `store.json`, `reload.json` and `stored-rotated.png`. The managed database restart preserved these exact two stored rows, all five remaining personal items, containers, appearance, actor and permanent access/location. `restart-comparison.json` records exact equality after automatic reconnect. `stored-after-process-restart.png` shows the actual reopened cargo inventory and rotated rifle after GPU drawing. Take all retrieved the same seven original item UUIDs, leaving cargo empty (`retrieve.json`). The account signed out, browser blanked and named session closed. The earlier `stored-rotated.png` captured a stale composited frame; use the later actual inventory screenshot as visual evidence.

This browser fixture predates the subsequent authoritative kit-replay/name correction (fresh generic r0006 smoke covers that correction). Its historical ship name therefore still includes the candidate label; new accounts created with the final factory use Wayfarer. Existing accounts retain original ships; this is not the requested existing-ship migration/refit yet. The one-deck template does not establish full multi-deck, airlock or routed-system completion.
