# Shipyard shared view and current-template inspection

Status: source implementation and real browser acceptance complete; dashboard publication remains parent-owned.

The inspection shortcut in `LayoutEditor` opens the pinned r001 Wayfarer source document (`362f37217f63a44a470676f104c8368bd0973ca03f5e29eab190bc4d6df71340`) through `createFromWayfarer` and `preserveBeforeTemplate`. It saves the current in-memory proposal using the existing recovery CAS path before creating a separate local draft. Recovery conflicts and quota failures block the switch. The resulting local document retains all211 native part placements and51 semantic floor placements. It is not a live capture: current game changes, extra fuel attachments and item contents are not imported.

Actual browser evidence: `output/playwright/wayfarer-editor-inspect/shortcut.png`. The shortcut preserved a renamed unsaved proposal, switched to Objects/3D, hid the roof, selected `equipment-locker--4.7--6`, and showed its actual unchanged native position East−4.6875m/North−6m/Height0.1875m. Browser console contained no errors after clearing a stale development-server transform cache. Four existing camera warnings remained. The named browser session was blanked and closed. Shortcut checkpoint: `0157b507`.

The follow-up removes mode-forced projection changes and transfers the same native camera between editing palettes. Structure, Rooms and Systems now draw the SVG floorplan using a projective transform derived from the native camera matrix; floor picking intersects the same deck plane before grid snapping. Explicit projection buttons still choose Top/Side/Front/3D. Content edits do not refit the camera; explicit Fit and a new document may do so. Middle drag orbits, right drag pans and wheel zooms the shared view. These are local authoring controls only.

Objects/Hull use the same configurable grid as floorplan editing:2m module lines and supported subgrid options. Existing native placements retain their exact original transforms on load; a user drag is the explicit snapping operation. Existing component yaw and Rotate90° controls remain available. A new early partition guard requires a continuous chain of existing shared tile edges, including valid diagonal edges; exterior-only edges, tile-centre spans and unsupported gaps reject without adding the invalid partition. The existing compiler remains the authoritative source of design diagnostics; none of this grants gameplay publication or live refit permission.

Focused checks pass: three native/CSS projection comparisons at varied camera orientations and source origins; three shared-edge placement cases; five existing template/recovery tests; dashboard TypeScript and independent dashboard build. The projection test tolerance is less than0.001pixel for Babylon's Float32 matrix composition. Actual browser acceptance now passes: exact alpha/beta/radius/target through all five tabs in both3D andTop; SVG orbit/pan/zoom; a document edit with no camera reset; projected floor selection; cabinet90° rotation retaining0.1875m elevation followed by Undo. Loaded native/overlay evidence was viewed in `output/playwright/wayfarer-shared-view/rotated-structure-ready.png` and `top-selected-floor.png`; exact numerical results are `acceptance.json` beside them. The browser had zero errors/four existing camera warnings, then was blanked and closed. No multiplayer or world schema changed in this slice.


The independent `layout-placement-contract.test.ts` review adds three parity/rotation/import-preservation cases. Two native grid tests ensure horizontal snap does not round away independent deck height or mutate imported input. The combined focused gate passes16tests/5files. Full `npm run check` passed TypeScript and1359tests, with the unrelated planet-terrain stress test exceeding its20second timeout; its isolated rerun passed all5tests in16.1seconds. This aggregate run is recorded as a timeout, not falsely reported green. The independent dashboard build passes; no world build/generation or authority publication was part of this UI change.

The older duplicate `Load existing Wayfarer` button is removed: it fetched the legacy assembly and adopted directly, bypassing the protected canonical-template path. Native grid dragging snaps XY absolutely while retaining its explicit Z datum. Semantic floor/fitting transforms retain their existing snapped-delta behavior and imported residual offsets. None of the six known locker/wall intersections are corrected by this editor change; their separate native candidate and source proof remain in [usable_boundary_wall_integration.md](usable_boundary_wall_integration.md).
