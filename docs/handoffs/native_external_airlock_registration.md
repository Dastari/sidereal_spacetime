# External airlock shared integration

2026-09-10 current status: shared parser, spawn mappings, private controller table, keyed view, native door dispatch and timer ownership are registered in source and published only to the named isolated airlock database. The normal public release remains worlda962/client2f5c. Root owns the paired renderer/client integration and actual browser acceptance. The earlier staged notes below are retained as the integration history; current evidence is appended at the end. Main proof and limitations: `native_external_airlock.md`.

1. `packages/content/src/construction.ts`: move the exact `NativeAirlockBinding` interface from the new sim document module into content; add optional `airlockRoom?: NativeAirlockBinding`. This binding includes exact pin, deck, two opening IDs, exhaustive 70-part source index mapping, 12 roof tile IDs and four exterior tile IDs. The shared model must never accept a bare `airtight` boolean.
2. `packages/sim/package.json`: export `./construction-airlock-plan`, `./construction-airlock-controller`, `./construction-airlock-document`. Keep the controller/plan independent of world registration. Publish the exact audit bytes through a new content module/export using SHA `eb7eab8523d68906a302a84cf06a5b2a507229d15e89fb96f66d1ade8b6e0aa6`. Source file validation occurs at build/install; a world runtime factory accepts only the exact pinned audit bytes without loading native GLBs into the world bundle.
3. `construction-transactions.ts`: add `airlockRoom` to allowed top-level keys and call the new exact validator. The current `readNativeAirlockDocument` delegates base validation to `readConstructionDraft` **after removing `airlockRoom`**, so maintain that direction to avoid recursive parser loops. Validate before UUID allocation or writes. Sort mappings deterministically after validation. Do not enable pressure readiness merely because a draft has a binding; the installed two-door authority must be registered in the same release.
4. `construction-instances-model.ts` and spawn identity remapping: enumerate/remap every identity from `nativeAirlockDocumentIdentities`, including native part IDs and both opening IDs. The two-instance remap tests supply the exact coverage list. Native sources are immutable shared data; placement IDs, chamber cells, door actuator state and gas stores are per instance. Compare actual server-owned placement lists with the compiled native installation before installing gas/door state.
5. Native floors/roof/boundaries: the exact fixture adapter owns all native placements. Use its 12 roof tile IDs to omit exterior roofs and prevent generic structure from being rendered or certified over the platform. The generic floor support footprint includes all 16 native tiles. Keep the boundary/door collider source separate from visuals. Do not use the generic semantic orientation for the outer door: its accepted frame is `(6,2)`, quarter-turn **3**, while its semantic opening spans X=6,Y=0.375..1.625. The inner door is `(2,0)`, quarter-turn **1**. That distinction determines hinge sweep and physical gasket support.
6. `construction-doors.ts`: install/bind both exact door frames and leaf colliders, with ordinary scoped proximity/revision/operation checks. All target changes for these bound doors must route through the airlock controller before either leaf moves; the existing unbound single-door reducer remains unchanged. Native seal/hinge state is per opening, not one `doorId` on the old single-door pressure row.
7. NEW world airlock adapter/table: private instance-keyed immutable binding plus accepted controller state, proof/install hash, two actual door IDs, and two gas cell IDs. Reuse the atmosphere store and bounded clock; default vacuum. Only active door/controller/gas changes write. Supply current power/grant/input/pressure/obstruction on consumption. Pending unsupported egress does not delete support or create access. An exterior platform is not a suit/docking/EVA capability. No pump gas route until real native port and conserved supply authority are qualified.
8. `packages/render/src/construction-instance.ts`: if exact airlock binding is present, load `loadNativeAirlockScene` before the generic floor/roof/wall branches. Inputs: instanceId, deckId, the exact 70 mapped installation parts, elevationM **0** for this fixed-datum fixture, exact seven source URL/hash pairs, and both accepted opening/frame bindings. Parent/world origin mapping remains unchanged; native floor top already exists in GLBs. Return that loader's placements, meshes, camera frame, walking elevation, `setDoors`, `setView`, and idempotent disposal. Do not mix in the old pressure-room loader or duplicate floor geometry.
9. `world/index.ts`, generated bindings and App/net: register keyed own chamber/pressure/door projections guarded by game admission, accepted instance/deck and read permission, plus normal target reducers. Preserve existing minimum egress/seat projections. Client controls emit intent only; renderer consumes accepted hinge/gasket state. Hidden actors/roofs and character camera mode confer no authority.

Before activation: exact native material/source checks, full check/build/art gates, fresh isolated baseline, then two fresh instances and real-provider intent/reconnect/permission/CAS/gas-conservation journeys. Actual browser review must show independent inner and outer morph/hinge motion, supported walk-through, roof/landing visibility, obstruction, pressure rejection and no public/private view leakage. No normal Wayfarer attachment/refit occurs until its own aperture and floor interface are separately qualified.

## Executable registration candidate — 2026-09-10

`packages/world/src/construction-airlock.ts` now implements a private, instance-keyed controller and a minimum keyed own-occupant projection. It is now registered in source and the isolated `sidereal-spacetime-dev-native-airlock-r0001-smoke` database; it is not part of the normal publica962 release. Its new tests use actual native geometry, the real construction permission/operation ledger, the real gas store/solver, and explicit host boundaries for admission/input-control.

The existing native assets have no qualified independent service-handle socket. This capability is therefore labelled **manual service operation**, not a motor, pump, powered door or invented visible crank. An accepted command binds one actor, connection/control lease and one door for at most three seconds. Each fixed step rechecks game admission, connection, input control, both workspace grants, accepted instance/deck, ordinary standing state, supported native position and clear nearby approach. It drives only that selected mechanism. Loss stops at the accepted hinge/gasket pose without moving the actor; reconnect never resumes an old operation. A fresh nearby validated command is required. Opening remains subject to actual differential pressure, the other door's fully deployed seal and native swept-body obstruction. Disconnected bodies still obstruct a moving leaf.

The new gas store starts at vacuum and installation replay preserves its contents/accounting. There is no refill or pump endpoint. Door closure, gas equilibrium and expired service become inactive, eliminating idle writes. The shared patch also scopes the older single-room gas solver to its admitted instances; two controllers must never step the same atmosphere row. Only actual changed hinge/gasket/gas/driver state advances the new controller clock.

Before registration, the workflow was `python3 scripts/stage_airlock_integration.py`, then `npx tsx scripts/validate_airlock_patch.ts` and `npx tsx scripts/validate_airlock_spawn_patch.ts`. The generator now refuses to overwrite already-applied source. Its historical contract records exact before/after hashes in `native_external_airlock_patch_inputs.json`, stages `native_external_airlock_integration.patch`, and never edits shared source. The compiler host reads the proposal in memory. The executable spawn gate bundles the same overrides and proves two fresh instances, 182 independent mapped identities, 140 native parts, four door identities, 32 floor identities, reload stability, closed barriers and both separately opened continuous native routes. Current candidate blueprint SHA: `713bf0c81011e85e8128ae4b26c1fd2ab4119ba024637429764931db1e8e7eae`.

The patch deliberately leaves readiness pressure/services/nativeDamage/flight flags false: the exact fixture is qualified, while a generic Shipyard-wide pressure/service implementation is not. The underlying seven GLBs match the already published pressure-room source URLs and hashes; the **new a007 composition proof** is independent. Do not substitute the old room proof. No new visual mesh or final artistic approval is implied.

Remaining integration gates:

- Coordinate world/index, parser, schema, instance and generic door ownership after the current flight proof. Refresh/review hashes; never apply an outdated diff blindly.
- Register the private table, keyed own view, target dispatch, single timer owner and accepted collision together. Generic door installation/ticks must skip this binding. Existing refit/damage APIs must not replace its immutable native document or proofs; no such airlock refit capability is currently exposed.
- Root owns renderer/App/net: load `loadNativeAirlockScene`, exact bound 70-part list and `NATIVE_EXTERNAL_AIRLOCK_SOURCES`; map both accepted seal fractions from `ownNativeAirlocks`, preserve floor datum once, and label the nearby action "Manual service". Exterior four floor tiles have no roof. Existing hinge-only projection must not imply a deployed seal.
- Run full check/build/art and fresh isolated baseline. Then real-provider publish/spawn/entry/service/reconnect/grant-loss/CAS journeys, including two instances and independent gas/door state. Source tests and the virtual compiler proof are not a substitute for these server journeys.
- Real browser review: both hinges and exported gasket morphs, supported route, ordinary keyboard walking, roof cutaway, exterior landing and obstruction. The landing is not an EVA/suit/docking capability. Initial vacuum is explicit; a powered pump and conserved upstream gas source remain separate work.

Validation at this staged boundary: `npm run check` passed 188 suites / 1,110 tests plus TypeScript and 76 document/provenance checks; `npm run art:check` passed. The focused airlock set passed 36 tests across five files (14 world adapter cases, seven controller cases, eight exact-document/collision cases, four native-plan/gas cases and three actual GLB renderer cases). The proposed shared registration typechecked in memory and the bundled two-instance spawn proof passed. Full managed world build/generation and isolated authority/browser journeys remain required **after** root authorizes shared registration; the current flight agent's database/module is preserved.


## Registered source and isolated qualification —2026-09-10

The reviewed13-file patch was applied only after matching every recorded before/after hash against current starter/access/name source. World compilation and generated bindings passed. The initial fresh isolated baseline passed on database `sidereal-spacetime-dev-native-airlock-r0001-smoke` (identity `c20071b6965ae49e4a9bbb459be36d327de0ff66ccb2ed173ce4690ac3c69047`). The native controller and new saved-origin relation remain private. Source tests now cover normal game access rejecting fallback to review grants, unregistered template denial, actual supported per-door approach and chamber-side closing.

`ownNativeAirlocks.innerCanService` and `outerCanService` use accepted native support, the same blocked-route test as command consumption, standing state, lease/presence and materialized game admission. These fields do not bypass pressure or interlock validation. The active browser additionally checks its own input lease; views cannot distinguish two tabs of one principal. `setConstructionDoor.expectedRevision` is the **selected door row** revision, not airlock status revision. Once manual service expires/stops, the UI can submit the same target with a fresh operation ID to resume. Commands and each tick repeat exact-time auth/control/grant checks.

A real provider walk exposed self-blocking service approach during chamber-side closure: the old0.7m offset sat inside the1.25m leaf sweep. The qualified service stance now uses1.6m offsets, clear of leaf plus0.3m body while preserving the2.5m service range. The physical geometry is unchanged. A regression covers full chamber-side closure and deployed seals.

### Native starter review transit

Permanent native starters already have a construction location. A new private `constructionReviewOrigin` relation preserves that exact original visit ID, deck ID, location revision, local XY, source revision/hash and character binding during an explicitly authorized construction review. This is a review transition, not physical boarding. Recursive visits, piloting/flight/traversal departure and unsupported origins fail. On return, current source/ownership/admission/access/support and other-body occupancy are validated again. Only actor and saved visit are restored: ship motion, inventory, grants and source revisions are never rolled back. Grant loss in the review cannot prevent a valid safe return; disconnected actors retain the receipt and must reconnect before return. Invalidated original support/access leaves the actor safely in review instead of teleporting through structure.

Actual independent native-airlock provider journeys and final browser evidence are still in progress at this checkpoint. Whole-Wayfarer attachment remains separate: the attached candidate SHA is not a registered trusted starter, and the durable access policy correctly rejects it. No entire-ship airtightness, pump, motor, refill, EVA or final artistic acceptance is claimed.

### Actual provider evidence and cleanup

On the isolated database above, `.runtime/native-airlock-review/journey.json` records two independent completed cycles: actor `283b70b4-4ef2-4f29-8493-8f54ca405c4d`, instances `d6052d42-f68a-410a-a382-304065dd1c85` and `3b76d366-dd70-4959-bfcf-6514b071ebdd`. Inner/outer IDs are distinct, both-open is rejected, ordinary intent walks onto the exterior landing and back, closed seals settle without idle revision writes, and vacuum/inventory remain unchanged. Private controller subscription is denied.

`.runtime/native-airlock-review/recovery.json` records a disconnect at accepted gasket retraction0.4/hinge0, no replay or idle writes after reconnect, explicit service resumption, grant-loss view removal and stopped actuation, and grant-free return to the exact original native visit `72ee7f7e-3c74-48ff-b313-6a698ae03663` on deck `15eb589c-4ff6-4055-be20-f1af9364be0f`. The private saved-origin table subscription is denied. Original ship is `d4368c77-ef84-469b-ac5d-7efd3901eec1`; original supported position is(-2,-2).

Temporary administrator role was removed through the managed lifecycle. A new real PKCE login had no administrator role and the actual grant-management reducer denied it (`role-denial.json`). Both specialist provider sessions were logged out with204 and temporary token/session files deleted. Ordinary fixture grants remain temporarily valid for root's separate browser review; root owns that browser session and its cleanup.

A final rerun after the stricter receipt replay gate overlapped root's browser login and stopped on chamber-side closure (`final-run.log`). Input-lease transfer is suspected but has not been independently proven; it is not counted as a passing final candidate journey. Root is reviewing explicit browser resume from that accepted partial pose. The earlier completed cycles and recovery evidence remain preserved, not overwritten with a success claim. The tighter replay gate has an actual authority regression: revoked current access cannot replay an earlier accepted service receipt. Current focused suite:82tests across6authority files; full art validation passed. Fresh full aggregate/build and root browser sign-off remain the release gates.


Root subsequently verified in the actual browser that explicit service resumes the interrupted inner close from accepted5% hinge/seal1. The renderer presents66native placement nodes plus4gasket placements and two accepted hinges without console errors; the complete browser walk-through is still being recorded by root. Full combined `npm run build` passed after registration and native review return wiring. Six legacy fixture regressions were corrected with real empty private-origin stores; the82-test focused authority gate includes those suites. Root owns the final aggregate check across concurrent refit/cargo changes before normal release.

Root full UI cycle then passed: resume partial inner motion; open outer; ordinary WASD onto exterior at aboutX7/Z0.1875; close/open outer; walk chamber; close outer; open inner; walk interior at about(1.1047,0.929)/Z0.1875; close inner with both deployed seals and zero accepted hinge fractions. Return restored native home and all7existing item rows, and the UI showed review targets instead of an incorrect Return button. Exact browser evidence: `output/playwright/airlock-browser-review/cycle.json`, `return.json` and companion viewed screenshots. Root is retaining final roof/reentry and aggregate release verification ownership.
