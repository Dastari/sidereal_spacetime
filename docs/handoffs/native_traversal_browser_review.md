# Native two-deck traversal isolated review

Status: actual isolated two-instance traversal, last-socket reconnect, privacy, full capability expiry, stock/inventory restoration and provider-role cleanup passed. No process restart is included. Root owns integration, aggregate gates, additive publication and provider-role lifecycle. The normal world and the independent pressure-room review remain separate.

`npx tsx scripts/native-traversal-review.ts prepare` validates the exact native document factory and emits the review functions under `.runtime/native-traversal-review/`. Preparation performs no network calls or authority changes. `scripts/traversal-smoke.ts` exports browser-compatible connection helpers; importing it performs no connection, grant, publication or actor mutation. Generated bindings must contain the traversal reducers/views, including server-derived `atLanding`.

The fixed target is `sidereal-spacetime-dev-review-traversal-r000`, workspace `native-traversal-review-r000`. Root additively publishes through managed lifecycle tooling and temporarily grants the existing private review account its construction administrator role. Credentials remain in `/tmp/sidereal-auth-review.json`; the generated sign-in helper records any echoed CLI command only in a private mode-0600 `/tmp` log. No token or password belongs in acceptance output.

## Prepared interfaces

- `traversalDenialSmoke(a,b)` is ready for root's isolated smoke runner. It subscribes to narrow views, rejects ungranted begin/cancel and verifies that all five private traversal base tables reject direct subscription. It creates no characters or inventory.
- `publishTraversalReview(authoringConnection)` grants four one-hour scoped capabilities through the actual provider-admin reducer, saves/publishes the exact document, and spawns two instances. It refuses to duplicate an existing review namespace and compares all instance, deck, link, native-part and aperture identities across both spawns.
- `traversalAuthorityJourney(gameConnection,[instanceA,instanceB])` uses an already entered, standing review character and ordinary `setIntent`. It walks to the landing, traverses up, walks on the upper deck, returns down, cancels midway, and returns to the original stock ship for each instance. It compares character identity, appearance, inventory state, items, containers and hotbar throughout. It does not initialize a starter kit, equip anything, inject coordinates, or debit/create resources. Use this on a dedicated authority connection: the visible app retains its own periodic keyboard input sender and sequence.
- `traversalRestartCheckpoint` and `verifyTraversalReconnect` preserve nonsecret state and assert the same visit/journey, a persisted return and an unchanged inventory. They do not restart a service or retain authentication. Root coordinates actual last-socket checks; process restart is deferred to a separately managed isolated daemon. Ordinary page reload is not process-restart evidence.

## Named browser procedure

Use the Playwright skill wrapper with the named session `native-traversal-review` and the existing `/tmp/sidereal-light-browser.json` launch settings. Confirm the shared GPU is free with root before opening. Evidence belongs under `output/playwright/native-traversal-room/`.

1. After root's candidate build, additive publish and temporary role grant, invoke `setup.js`. It opens the actual Shipyard application with browser-local transport substitution to the fixed isolated database. It preserves real authority websocket traffic, blocks HMR for capture consistency, pauses automatic RAF, and exposes existing scene/canvas references for observation. No state, position, collision result, permission or reducer response is mocked.
2. Sign in through the actual provider form. The generated `signin.py authoring` helper is for a currently visible login form and does not open the browser. Invoke `publish-spawn.js`, saving its blueprint hash and two complete placed-identity sets.
3. Invoke `game.js`, sign in using the same dedicated account, and enter the isolated game through the ordinary UI. Stand up through the actual control before review entry. Preserve the initial stock ship/return position and inventory. Invoke `observe.js` after ordinary initialization; its original fixture snapshot is stored in session storage so reload cannot silently replace the baseline.
4. Enter the first review instance using its rendered control. Use real keyboard walking to reach the ladder. `keyboard-walk.js` accepts a bounded `window.__reviewKeyboardPlan={keys:['KeyW'],durationMs:500}` and drives the app's actual key listeners; adjust keys only from observed movement/camera orientation. Re-snapshot DOM/canvas controls as necessary. The server's `atLanding` flag controls eligibility, and the begin intent contains only link, visit and revisions.
5. Capture lower-deck geometry and a blocked native guard/edge approach. Use the real ladder button for the principal visible acceptance path; `begin.js` and `cancel.js` provide ordinary reducer helpers for focused denial/latency checks. Capture accepted XYZ at intermediate heights with the source deck still effective, the actual shaft/openings/guard, arrival on the upper deck, ordinary upper-deck walking and a blocked approach to the unsupported shaft. Traverse back down. `capture.js` renders complete existing engine loops before saving a screenshot; no FPS claim follows from these controlled frames.
6. Cancel a second traversal midway. Confirm continuous return to its accepted source position, one completion and released reservation. Begin another and run `checkpoint.js`, then close **every** socket for this principal, including any authoring tab or helper connection. Ordinary reconnect must show the same visit/journey returning or already returned, with no inventory change. A page reload alone does not prove the last-socket case if another authenticated tab remains open.
7. A process restart is excluded from this run: the managed daemon also serves the normal world. Record ordinary last-socket/reconnect behavior and the stateful adapter serialization/reload tests separately. Actual process-restart acceptance requires a separately managed isolated daemon later; do not run `restart-database` for this review.
8. Repeat visible enter/walk/traverse/return in the second instance. Confirm distinct identities and that the first instance is unchanged. Run `privacy.js` for an independent ungranted principal and its same-principal reconnect; both must see no private traversal/link/instance/visit rows. Tokens remain inside that helper's closure.
9. Capture standing begin arguments with `remember-expiry-intent.js`. Using a separate actual authoring session while root's temporary role remains valid and the actor is standing, schedule the four review capabilities to expire shortly after an ordinary climb begins. The actual review used 5.5 seconds. Capture `returning` with `access-lost` and accepted XYZ still visible while links disappear; after the safe return, `verify-expiry.js` requires an actual begin denial mentioning its grant and empty traversal/link views. Return to the stock ship using the ordinary return path, which remains available after grant expiry, and run `inventory-check.js` against the original baseline.
10. Root revokes the temporary provider role. Fresh authoring login must fail a new self-grant. Restore controls, remove review-only session-storage checkpoints after evidence is saved, sign out, navigate to `about:blank`, close the named session and report the GPU free.

## Evidence boundaries and adapter follow-up

The world adapter's focused tests cover native source/document/map mismatches, permission-before-replay, stale revisions, two-character contention under the existing owner policy, per-instance isolation, both directions, continuous cancellation/disconnect/grant-loss return, persisted state reload, source revision invalidation, blocked geometry/reservations and private projections. Cancellation now accepts a positive observed revision no newer than the current revision for the exact immutable traversal and visit; it rejects future revisions and old-journey reuse. Server-derived landing eligibility uses the full actor footprint. Ordinary movement checks the swept body AABB against reserved footprints, including diagonal corner grazes.

The fixed fixture and bounded authority budgets are intentional. A read-only review agent measured approximately 0.24 ms per native compile for this fixture; this bounded local profiling sample is not end-to-end latency or hardware acceptance. No broad caching/performance change is included. The permanent terminal audit admits at most 512 completed/pending traversals per owner before explicit archival policy is needed, alongside the existing 4096-operation receipt limit.

Record results only after execution: module identity/build, source hashes, blueprint hash, both identity sets, original/restored stock ship/character/item identities, ordered accepted-state traces, actual screenshots, observed collision limits, capability expiry and fresh-login denial, browser last-socket reconnect evidence and final cleanup. This fixture has no qualified pressure or gas charge and establishes no powered lift, life support, survival behavior, arbitrary traversal adapter, or completed Wayfarer/multi-deck milestone.


## Initial actual capture — 2026-09-09

Root published isolated module `sidereal-spacetime-dev-review-traversal-r000`, identity `c200eee63ba084cb79bdb3e4780767e97c5b6fb1605a9f079caf1665c808983f`, then granted the dedicated review account its temporary authoring role. Actual authoring reducers published blueprint `eca46097-ce7d-4e2c-8250-2f1e10153797`, SHA-256 `ad61f3869efba3cf94697a13476a9e3ee7eb720f5fc0c1a702e0dc0e530ae363`. Instances are `a5e6a5d1-239e-42ed-a8a3-b69a90ac64bc` and `33a46bbc-53a6-4bbb-8f59-af64fce4e918`; `publish-spawn.json` records their distinct deck/link/28-native-part/two-aperture identities.

The first instance was entered through the rendered control. Normal keyboard movement reached `(3.0873768571141182, 1.1251112049931635)` and enabled the server-derived landing flag. The actual Climb button produced 133 sampled states through revision 146, including approach from that original position, accepted vertical progression and upper arrival. Lower deck stayed effective until the terminal location revision advanced to 2. The downward Climb button produced 135 samples and returned to the lower deck at location revision 3. The actual Cancel button produced 85 samples, including `returning`/`cancelled`, then returned to `(3,1.25)` on the original lower deck at location revision 4. `initial-trace-audit.json` verifies revision ordering, source-deck preservation and displacement within 0.04 m times the observed revision gap.

Actual upper-deck keyboard walking reached the west side of the platform; attempting to cross the native shaft guard stopped at approximately `(1.5618617150099117,3.137111465562428)`, before the nominal hole edge at x=2 m. The character then walked back to the landing. Oblique screenshots `room-a-mid-up.png`, `room-a-upper.png`, `room-a-mid-down.png`, `room-a-upper-guard-collision.png` and the cancellation captures show the actual rungs, guarded opening and accepted character elevation. The renderer audit identifies all 28 placed native IDs and 101 clone primitives. The existing standing character pose is transported; no authored climb animation or armor-fit acceptance is claimed.

`fixture-before.json` records isolated character `c213182a-d663-4026-a878-31e95e117c18`, stock ship `e67e539a-7419-45f7-b56a-b3dfea1631b0`, return position `(0,10.25)`, seven item UUIDs, containers, hotbar and appearance. This fresh isolated fixture was initialized only through the ordinary game entry control. Normal-world characters and inventory were not used. Final preservation passed against that exact baseline; provider-role cleanup is recorded below.

All evidence is under `output/playwright/native-traversal-room/`. Frames were rendered on ANGLE Vulkan SwiftShader with controlled complete engine loops; the results establish behavior and geometry visibility, not hardware performance.


## Final candidate capture — 2026-09-09

Root additively republished the same isolated module after retaining the admitted owner's accepted traversal position/status through workspace grant loss. Final published world bundle SHA-256 is `9f383ec3f04a9bb9878062f17c76e387297f69ddac5b23945e63374bfea0a1ac`. The initial capture's bundle hash was not pinned. The final browser was reloaded before the remaining checks, and current runtime sources remained frozen during capture.

`ui-disconnect.json` records a real last-browser-socket reload during ascent: the authoring tab was closed and no helper connection remained. Traversal `f6ce2a7b-1d02-4617-bde9-a4704d008a90` was checkpointed at z=2.2175 m, revision 83; the first reconnected row retained the same visit/journey, reported `returning`/`disconnected` at z=1.6975 m, revision 97, and retained the original lower deck. Ordinary game entry resumed presence and the actor returned to the source anchor. This is browser disconnect/reconnect evidence. The adapter's serialized-state restoration tests are separate; no database process restart was performed.

The second instance was entered through the actual control and approached using ordinary keyboard movement. Its rendered upper Climb button produced 137 samples, upper-deck keyboard walking worked, and the lower Climb button produced 139 samples returning to the lower deck. Evidence is `ui-climb-up-b.json`, `upper-walk-b.json`, `ui-climb-down-b.json`, `room-b-mid-up.png`, `room-b-upper.png`, `room-b-mid-down.png` and `room-b-lower-return.png`. Both rooms retain their disjoint native/deck/link/aperture identities. `privacy.json` records an independent ungranted principal with no character/items: both its initial subscription and same-principal reconnect exposed zero traversal, link, instance and visit rows; unauthorized begin was denied.

`expiry-return.json` records all four actual workspace capabilities expiring during a rendered-button ascent. Its 128 sampled states include automatic `returning`/`access-lost`; links disappeared while the admitted owner continued receiving accepted XYZ and status. One synchronous observation reported accepted `(3,2.5,1.617500000000002)` and actual renderer actor-root `(3,1.6174999475479126,-2.5)`, consistent with world-to-render mapping and graphics float conversion. `full-grant-expiry-return.png` shows the elevated actor and guarded ladder during return. The source deck remained effective; the terminal location advanced to revision 6 at `(3,1.25)`. A new begin using the previously observed valid intent was denied with `Explicit active construction grant required`, leaving no active traversal or link rows. Direct self-admin grant mutation during an active traversal was correctly rejected by the existing standing-action guard; expiry was scheduled while standing, with no bypass or mocked permission.

`final-return.json` records the real Return control restoring character `c213182a-d663-4026-a878-31e95e117c18` to stock ship `e67e539a-7419-45f7-b56a-b3dfea1631b0` at `(0,10.25)`. Exact original item/container/hotbar and appearance/state snapshots matched, and all four scoped grants were revoked by expiry. Root completed managed provider-role revocation. `fresh-login-denial.json` records a new actual provider login with the administrator role absent, an actual self-grant reducer denial, all four grants still revoked and empty draft/blueprint projections. `cleanup.json` records both apps signed out, review-only session checkpoints removed after the final inventory comparison, and both tabs blanked. The named browser was then closed and the GPU released. `acceptance.json` consolidates the final candidate identity, source pins, placed identities, exact restoration and evidence-file hashes.
