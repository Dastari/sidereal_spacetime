# Native ship integration candidate

Status: ship integrations installed and reviewed in the normal local game. Final combined build and native volcanic iteration remain in progress; this is not final art sign-off or a production release report.

Owner authorized the cumulative R006 pilot hull live, all 73 exact approved cargo appearances in the local game, and floor r002 in a combined local candidate. Floor final art sign-off remains absent. Root owns catalogs, loaders, assembly, publication and combined verification. Authority/UI is delegated to authoritative_ifcs; material experiments to geometry_snap_bevel; native planet revisions to procedural_planets. External character/shipyard/model work remains independently owned.

| Change | Exact source | Current gate |
| --- | --- | --- |
| Pilot hull | R006 recipe and editable source; hashes in hull-installation.json and runtime hull manifest | Native assets merged by placed ID; actual Deck/Flight, doorway, piloting and collision checks passed |
| Deck floor | floor r002 shared kit, twelve variants | Candidate installed; lower structural backing retained, fitting proxy clipped at 0.1875m; separate R006 authority collision revision 3 installed |
| Cargo | Approval manifest SHA-256 512ea3056138b3da315bb92e681c01ba5d758d54928f74d986b58bd9b001e4dd; 73 mixed revisions | Hash-checked durable sources; four storage replacement identities preserved |
| Storage inventory | Existing private inventory plus additive placed-container bindings | Normal local database published additively; actual pointer store/retrieve, reload persistence and stale out-of-range rejection passed |
| Plastic finish | R006 scoped material derivative, retained floor ORM | Study04 locally installed after root image review; approved base preserved; full plastic look not accepted |
| Ice world | Native r015 Blender kit and seven GLB form equivalents | Local working candidate installed and actual Observe reviewed; white-crust/blue-interior improvement, reference fidelity remains open |
| Volcanic world | Native prototype in progress | Agent preparing actual Cinder seed89 comparison; not installed yet |

`scripts/install_release_candidate.py` installs pinned cargo/hull/floor metadata. It archives the pre-integration catalog, default assembly and manifests once in `assets/source/archive/pre-native-ship-2026-09-08/`. It merges only named placements and retains unrelated catalog entries. Native visual geometry is not replaced with voxel proxies. The archived default is recovery evidence, not permission to overwrite newer edits or the database.

Remaining gates: final combined checks/build and artifact hashes, volcanic visual/runtime acceptance, and completion of the release evidence record. Assembly fitting, fixture reconciliation, cargo persistence, native Deck/Flight review and isolated authority smoke have passed. Performance evidence is geometry/draw-count comparison; no recovery to a particular hardware FPS is claimed. No production deployment is implied.

## Current integration evidence

- Exact native artifact validator passes for 73 cargo assets, R006 hull and r002 floor. Shared kit preserves normal maps, tangents and material channels. Asset validation, floor fitting and published hull placement checks pass.
- Four storage pods use floor layout revision 2: old lower IDs at X=-4.0625, old upper IDs at X=-3.3125, revised Y rows 2.0625/3.4375, Z=0.1875, rotation +PI/2. Original placed IDs, bound container UUIDs and contents persist. No filled stacking or animated lid clearance is claimed.
- The normal local database received non-destructive additive updates. Actual pointer store/retrieve and reload persistence passed with the final four-pod layout. A later stale out-of-range transfer was rejected without changing the inventory revision. Original item, fitting and container UUIDs remain intact.
- Static native cargo and hull batches group by exact material AND vertex-channel set. Each placed object retains an independent selection/gameplay identity. No texture channels are stripped.
- Combined ship/native-ice checkpoint: 71 test files / 263 tests, typecheck and 64 documents passed. Full build, art validation, native artifact audit, published hull placement, floor fitting and art-library checks passed. Logs are `.runtime/native-release-{check,build}-final-ship.log`, `native-release-art-final.log`, `native-release-floor-final.log` and `native-release-library-final.log`. Volcanic candidate changes require their own final checks before inclusion.

## Installation and recovery contract

For this pinned candidate, install metadata in order `hull`, `cargo`, `floor`, `seat`, `floor-fitting`, `cargo-layout`, `finish`, then `record` using `scripts/install_release_candidate.py`. Historical seat alignment is recorded as pilot layout revision 2; current authority revision 3 additionally aligns bow crew clearance and the ship contact capsule. Cargo layout requires matching server-authored storage fixture coordinates. Publish authority only through the managed non-destructive `npm run world:publish`; never reset the normal database. Build/copy client and dashboard independently. A new account or database address is not a persistence test.

Recovery is a scoped forward repair: restore only changed visual asset mappings/placements from the recorded before/after mappings while preserving subsequent unrelated edits. Do not restore the entire archived ship or database. Keep additive container bindings and existing inventory rows intact; a visual rollback must not delete cargo or duplicate grants. If repair restores pre-layout cargo or hull placements, apply a matching validated forward authority repair for reach and collision coordinates; a visual-only rollback cannot leave mismatched authority coordinates behind. Floor r002 remains a review candidate, not final owner art sign-off. No production deployment has been performed.


Finish study04 is now in the local candidate. Fifteen native hull GLBs use separate `hull/finish-r004` URLs; immutable approved R006 files remain at their previous paths. Manifest `approved_visual` fields and `finish-installation.json` provide exact before/after hashes. Runtime exterior lighting uses radius0.04, 1024 PCF low filtering, depth bias0.0035 and normal bias0.015. Room lights and global HDR intensity are unchanged. Matched study counters showed86 active hull meshes versus369 before compatible material batching; full scene draw calls868 close/1285 whole include shadow passes and are not isolated hull draws or a hardware FPS promise.

The combat specialist's r002 is wired behind development URL `?poseReview=r002` and passed actual-game integration review: accepted shots consume energy, weapon switching preserves identities without replaying historical recoil, and the beam starts at the actual muzzle. Canonical assets remain unchanged. Root also fixed a same-frame ancestor-transform cache error and lost quick clicks in the canonical path; actual canonical click accepted exactly one shot and muzzle error was below 1e-16m. Native floor contact is aligned at 0.1875m. Stock/optic fitting and acquisition timing remain specialist limitations, not finished combat art. See `docs/handoffs/combat_pose_parent_acceptance.md`.


The installed finish's geometry cost is 35,968 triangles across the affected placements versus 10,336 for approved R006 (+25,632). The smaller +1,008 number describes only the last study04 increment over rounded02, not the total installed change. Independent geometry audit verified all15 canonical/public hashes,23placements and retained approved originals.

Native ice-r015 passed root's working image gate and is installed as a local review candidate in canonical runtime assets and both applications. `ice-installation.json` pins the authored native kit and seven GLB equivalents; immutable source snapshots are under `assets/source/native-planets/ice-r015`. Actual full Observe review passed, including pointer orbit/zoom and Return to ship with state preserved. Body selection for this capture used the real WorldHandle after a paused UI pagination timing problem, so this capture does not independently validate the destination button. The resumed planetary work prioritizes ice and volcanic improvements; other families remain next-in-line, with the accepted temperate baseline retained.

R006 collision revision 3 is installed in the normal local database. Crew clearance now accounts for the sloping canopy at a conservative 2.25m sample height, rear partitions block passage outside the single 1.25m doorway, and the forward-offset ship capsule includes the longer nose without moving the center of mass or aft limit. Thirty-two focused tests and final isolated smoke passed. Actual keyboard movement stops at the diagonal and rear jamb, crosses the doorway in both directions, and reuses the original helm without changing camera mode. Exact nose/straight bounds are covered separately by pure/isolated tests. See `collision-installation.json` and `docs/native_ship_collision.md`; final evidence is `output/playwright/native-r006-final-{bow-deck,roof-flight,doorway}.png`.
