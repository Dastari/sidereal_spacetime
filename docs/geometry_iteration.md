# Ship geometry and placement iteration — 2026-09-08

Status: implemented and CPU-inspected; integrated Babylon deck/flight and dashboard review belongs to the lead's current GPU session. This is visual geometry and local authoring validation, not live collision, pressure sealing, damage authority or functional socket attachment.

The supplied RPG after, top-down after, core construction block references and owner's z-fighting attachment were inspected. The specific translation is modest manufactured edge rounding on the existing solids, continuous floor support, unique ownership of overlapping semantic matter and correct item placement. This preserves the current ship proportions and independent object IDs.

`resolveVoxelOwnership` gives props/drives, markings, deck, walls, partitions, cutaway walls, exterior armor and roof a deterministic ownership order. Every sampled cell has exactly one semantic owner before ship export or assembly segmentation; object layer names and placed IDs remain separate. Removable roof/cutaway layers yield shared cells to permanent support so hiding a roof cannot erase its supporting wall. The authored pressure/collision model is untouched. Surfaces meet at cell boundaries rather than using polygon depth bias. The default assembled ship is exhaustively checked for occupied overlap, including structurally interlocking parts whose bounds intersect.

The Blender exporter now indexes coincident vertices and splits long greedy rectangle edges at collinear vertices before beveling. This closes greedy T-junctions at chunk and palette boundaries. Convex manifold edges at least two sample widths long receive a 0.02 m, two-segment bevel (finer parts scale to their own pitch); coplanar palette changes, concave sockets, small detail edges and nonmanifold endpoints are excluded. Palette corner colors and material roles survive export. Occupancy remains unmodified. Evaluated topology is checked to reject any bevel that introduces new nonmanifold edges. The ship retains 219 pre-existing nonmanifold edges from voxels touching along edges; no new ones are introduced. Those edges remain unbeveled rather than accepting cracks.

Initial indiscriminate eligible-edge export was 470,298 triangles. Restricting the bevel detail budget reduced the ship to 284,164 evaluated triangles, with 22,328 beveled edges; the assembly asset library is 345,786 triangles, with 28,958 beveled edges. Ship occupied matter is 1,789,545 cells across 547 chunks and 40 semantic layers. The underlying greedy mesh remains 47,018 quads / 94,036 triangles. Runtime surface metrics are separately tracked and hash-checked; the art gate permits at most 325,000 ship triangles and less than 3.5 times the greedy triangle count. These are geometry budgets, not measured frame-rate claims.

Two physical forward-facing retro mouths were added under stable layers `drives-retro--1` and `drives-retro-1` at X ±5.5 m, Y 8.5 m, Z 0.75 m. They exhaust +Y and align with authoritative IFCS reverse thrust. They add no pilot controls.

The dashboard validates every proposed add, move, duplicate, rotate, flip and restore using immutable occupied cells, including per-placement removed-cell drafts. AABB tests only narrow candidate pairs; the rejection uses occupied-cell intervals. Face contact is permitted. Mixed 0.0625 m / 0.03125 m assets and quarter-turn/mirrored interval transforms use the same rule. Placement snaps to 0.03125 m, and palette drops find nearby support surfaces within 0.5 m of the build plane. Failed drags restore the original render position and report the conflicting placed ID. Existing unsupported saved drafts remain preserved. No entire legacy draft is rewritten to fix its transforms.

CPU review: `output/playwright/geometry-bevel-review.png` was rendered and inspected. The continuous floor, crate cap edges, bed frames, sofa cushions and wall panel edge catches are visible. This is a Blender material/geometry review, not evidence of correct Babylon rendering. The current sparse furnishings and wall relief are still visibly simpler than the reference target; this slice does not claim completed premium-art fidelity. Lead owns the integrated screenshot comparison and missing-floor renderer diagnosis.

Validation: canonical `npm run art:voxels` and `npm run art:assembly` regenerated outputs. Placement tests cover solid overlap, hollow interlocking bounds, contact, rotation, mirror, grid rounding, nearby floor support and the complete default assembly. Final check/build/art-check results are recorded by the integration lead after all concurrent work.

Final local gates passed: `npm run check` (63 tests), `npm run build` (both applications and world), and `npm run art:check` (284,164 ship triangles, 112 primitives, 21 textured steel primitives, 213 reusable assembly assets, all manifest/palette checks). Only the existing Vite chunk-size advisory remains in build output. No authority code was edited by this slice.
