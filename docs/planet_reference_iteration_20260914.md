# Planet reference iteration — 2026-09-14

Status: nine main-world working art/morphology passes and 19/19 individual moon working visual passes at inspected views; hardware, runtime integration, shadow defects and final owner approval remain open.

The owner explicitly reopened planetary reference alignment on2026-09-14 and requested repeated independent Astra visual review until both author and reviewer judge the gap meaningfully closed for every remaining family. This supersedes the earlier planet-art pause for this task. It does not authorize unrelated rendering features or authority changes. Preserve native Blender sources, original reference crops, prior revisions, existing materials and body identity. Final owner art approval remains separate from the requested two-agent working gate.

Reference: original `reference/art/planets.png` and the exact per-family main-world/moon crops in the art library. The detailed family briefs remain in `planet_next_passes.md`. All candidates must meet `planet_lod_authoring.md`: worker preparation, build-ahead, retained ready levels, compatible shared/precompiled materials, no threshold-driven rebuild of fixed-detail geometry, and Flight/Map Observe transition tests. Visual comparison is not performance acceptance.

## Review queue

| Family | Current isolated candidate | Current gate |
| --- | --- | --- |
| Desert | Native r014 | Root + Astra main-world visual pass; three shadowed seeds inspected; hardware pending |
| Rocky | Native r010 | Root + Astra main-world visual pass; broader seed/hardware checks pending |
| Ocean | Native r007 | Root + Astra main-world visual pass; corrected optical/shadow and hardware checks pending |
| Toxic | Native r007 + fog r005 | Root + Astra main-world morphology pass; optical refinements and hardware pending |
| Crystal | Native source r009, composition r013 | Root + Astra main-world visual pass; coverage/visibility tests across three seeds; hardware pending |
| Gas giant | Native r005 | Root + Astra main-world visual pass; ring cast-shadow and hardware verification pending |
| Ice | Native r026, selective glass | Root + Astra morphology and selected-shard transmission pass; cap shadow hatching and hardware gates remain open |
| Volcanic | Native r023 + restored existing smoke | Root + Astra main-world visual pass; hardware pending |
| Temperate | Native r003 | Root + Astra main-world visual pass; broader seed/hardware checks pending |
| Moon variants | Individual candidates; Crystal1/2 r010, Volcanic2 r006 | Root + Astra working passes for all 19 individual moon crops; all-seed/hemisphere, optical defects and hardware gates remain separate |

Every meaningful candidate needs immutable source/export/render artifacts, reference/previous/current comparison, both agent assessments and recorded unresolved gaps. At least three seeds and all retained LOD levels must preserve the intended macro features. Hardware timing and normal game Observe evidence remain required; software renders prove appearance/counts only. Do not mark families complete merely because a shader compiles or a screenshot exists.

Review reports and repeatable captures: `output/playwright/planet-reference-20260914/`. Candidate provenance and revision history remain in the existing art-library design ledgers.

## Iteration log

### Desert r001 — rejected by both agents

Fresh current-renderer seed38 baseline captured at900×900 with the shared environment, HDR, sun and shadows in an isolated Babylon viewer. The browser uses SwiftShader; still captures are appearance/count evidence, not hardware timing. Both author and independent Astra found excessive full-surface tiling and absent macro hierarchy.

A new Blender native kit introduced sand ground, capped mesa groups, cliffs and three separately authored ground levels. Seeded worker composition preserved exact native triangles and placement ranges. The completed r001 render reduced fine tiling but failed: dark isolated towers appeared attached to a faceted globe; broad integrated aprons and regional terraces were missing. Both agents rejected it. Full source, validation, failed startup capture and final actual render are preserved under `output/playwright/planet-reference-20260914/desert-r001/`, with review reports under `reviews/` and evidence copied into the desert ledger r001. No runtime publication occurred.

r002 is in progress: broad basal shelves and intermediate ledges, a dominant cluster with smaller secondary formations, wider shallower canyon transitions, and reduced tiny scattered fragments. Main-world coverage only; independent review found desert/ocean/gas moons require distinct crater-based composition rather than automatic copies of their main-world morphology.

### Desert r002/r003 — technical defect isolated, visual gate still fails

Both agents rejected r002. A single-kit diagnostic then demonstrated that the isolated new importer used the wrong face-winding setting: outward Blender caps were culled, exposing interior faces. Setting explicit native CCW orientation restored solid roofs, pale caps and aprons without retinting materials. Astra independently inspected both diagnostic images and confirmed the correction. This defect was in the new isolated candidate importer; no existing public renderer was changed.

r003 preserved the exact r002 Blender kit, fixed winding, removed quantized radial contour bands, and exposed connecting feet. Actual whole-planet review still failed: excessive smooth empty ground and repeated isolated plinths. r004 now targets an authored irregular cliff/basin module assembled into connected regional escarpments, with two-angle and gameplay-scale review required. Historical failures and their source scripts remain preserved; no family has passed the requested two-agent gate yet.

### Desert r004–r006; Rocky r001–r002 — rejected, no publication

Desert r004 exposed a native topology integration hazard: long planar cap triangles became visible chord wedges under spherical mapping. r005 adaptively tessellated the authored surfaces in Blender without changing their material areas (maximum reported relative difference1.9e-8). Wedges were resolved, but both reviewers rejected thin belts on bare ground. r006 widened and rotated regions; both rejected shallow beige plates and absent frontal monumental relief. A shadow-off diagnostic removed fine hatching, isolating it as shadow-dependent rather than establishing geometry coplanarity. r007 now uses newly authored broad irregular landmasses with attached heads and branching canyon cuts.

Rocky r001 was rejected for raised cone-like craters on a bare sphere. r002 lowered bowls and deformed the native substrate but its actual render lost the main cavities beneath surrounding geometry; both reviewers rejected it. r003 is testing a single bowl's clearance before recomposing the hemisphere, with explicit exclusion of shelves across crater openings.

Ocean r001 native island/coast/atoll/grove source and wholeplanet capture exist; the first composition remains under review. Its tiny scattered islands do not yet establish the reference's archipelago scale. None of these candidates has passed the two-agent gate or hardware LOD acceptance. All captures in this entry use SwiftShader for appearance only. Runtime production files, authority and public deployment remain unchanged by these candidate iterations.

### Regional terrain and material iteration — continuing, no family accepted

Desert r008 improved column bundles but failed in two views: disproportionate repeated heads and empty midgrounds. r009 introduced connected low/mid fields and removed the synthetic radial canyon deformation. Both reviewers still rejected flat tabletop/cross topology and pinched geometry. A .006 normal-bias diagnostic removed the fine shadow hatching; this is isolated evidence, not a production shadow-setting change. r010 replaces broad plates with an explicitly authored twenty-two-terrace relief layout and is being checked as one mapped region before complete-body review.

Rocky r003 single-bowl renders at two angles established visible floor/sidewall clearance. r004 was a targeted camera-facing distribution diagnostic, explicitly excluded from final all-angle acceptance. r005 uses twelve globally distributed major craters and a revised native rim. Two-angle review still failed: blank substrate and repeated rims need integrated battered-crust regions. The next source revision targets that missing middle scale.

Ocean r002 failed in two views for pillar islands, nearly absent shelves and bead-like clouds. Authoring validation found a cumulative coast-width mutation that collapsed intended shelves. r003 corrects that source bug, lowers and widens five-part archipelagos, and uses Blender-tessellated native geometry for radial coast placement. Actual wholeplanet review remains pending. Toxic r001 also failed: recolored tabletop islands on uniformly lime liquid do not reproduce connected chemical basins, olive crust and irregular gas banks. Crystal r001 native source and a shared native cloud kit exist but have not passed complete-body review.

The isolated importer now preserves optional authored emission strength/color and IOR in addition to linear albedo/roughness/metallic. A NullEngine test checks those values. Native composition tests compare retained macro vertex positions across LODs, not just IDs. Full `npm run check` passed334 test files/1,993 tests and88 documentation checks; `npm run build` passed, with the existing bundle-size advisory. These checks do not establish visual or hardware transition acceptance. No candidate has been published.

### 2026-09-14 — material fidelity and continued native review

The read-only native attribute audit covers 63 variants. Supplied positions, normals and UVs match GLB data within explicit export tolerances. Crystal r002 alone omitted its JSON IOR 1.48 in 20 GLB material occurrences; preserved successor r003 adds that metadata without changing GLB BIN bytes, kit geometry or Blender source. Several older JSON kits omit authored normals/UVs entirely, so their runtime paths still require attribute-preserving successors. Gas and Ice have complete channels. Audit evidence: `output/playwright/planet-reference-20260914/attribute-audit/tolerance-verified/`.

Desert r012 establishes sufficient low/mid terrain coverage for selective finishing, according to the independent Astra review; compact summit masses and material detail remain open. R013 is being authored with native UVs/normals and generated sandstone albedo, preserving both the first generated cap and the corrected flat-pigment successor. These are ordinary PBR inputs, not a replacement shader.

Gas r002 actual browser views meaningfully close the body pattern/color gap according to both root and Astra. This is a body-only working subgate, not final family approval: the rings remain too regular and luminous. The next bounded iteration preserves the body texture and changes ring dust density, band hierarchy and debris grouping. Ice r016 actual diagnostic views confirm shaft depth survives an angle change; whole-planet views fail because isolated washer-shaped snow regions sit on a bare sphere. R017 must connect a shaft, open glacial cut and substantial unequal columns in one authored region before population.

Ocean r005 broadens native asymmetric shores and groves; its compositor expands horizontal footprints by 1.4 while preserving radial heights. Five tests, including NullEngine plateau picking at every LOD, pass. Cloud r002 fixes flattened banks and seed-correlated placement. Crystal r003 and Volcanic r019 await actual whole-planet review. No new family has passed the complete gate, and none of these candidates is published. Current captures use SwiftShader: appearance evidence only, not hardware transition timing. Hardware Flight and Map→Observe acceptance remains required after runtime integration.

### 2026-09-14 — first working visual pass: Desert r014

Root and the independent Astra reviewer agree Desert r014 meaningfully closes the main-world reference gap in seed38, two angles and an actual approximately 300-pixel body view. The source defect behind persistent summit rods was variable shadowing: regional terrace authoring overwrote the outline consumed by summit generation. R014 isolates the correct centered profile and adds selective erosion mouths while preserving r013 material/texture bytes. Residual coarse cap rhythm and subdued rim are refinements, not blockers for this bounded working gate. This is not owner approval or acceptance of the two desert moons. Native sources and browser evidence are preserved under `desert-r014/`; independent findings are in `reviews/desert-r014-volcanic-r019-review.md`.

High detail is 188,432 triangles, including the 81,920-triangle authored ground. The retained-geometry test now uses this actual revision, bounds it below 190,000 (previous generic bound 220,000), and compares regional positions, normals and UVs across LODs. An initial 120,000 estimate failed and was corrected from measured topology; no source decimation was applied. Hardware timing remains unmeasured.

Further findings: Gas ring darkness partly came from missing `twoSidedLighting` when reproducing glTF `doubleSided`; visibility alone is insufficient. The review importer now matches Babylon's glTF behavior with a NullEngine regression test. Generated dust RGB remains unchanged, with native material alpha exported separately from black density. Ocean r006 water/reefs pass working subreviews; r007 addresses only shoreline and land finish. Ice r018 earns a local morphology pass after source side-view comparison confirms the apparent ribbons were foreshortened solid prisms. Whole-planet composition follows. Toxic r002 has a real substrate/basin intersection; that is being fixed before material acceptance. Volcanic r019 and Crystal r004 remain open for regional continuity/finish and local illumination respectively.

### 2026-09-14 — Ocean and Gas working passes; shadow evidence correction

Ocean r007 and Gas r005 receive bounded main-world visual passes from both root and Astra at the inspected seed/views. Ocean retains r006 water, reef, groves and composition while adding island-only finish. Gas r005 preserves r004 geometry/materials/texture bytes and broadens granular density through authored radial UV support. These passes exclude owner approval, moon variants, hardware transitions and broader seed/optical verification.

The isolated viewer's old disabled-LOD shadow-list filter was defective: it removed staged meshes before activation and did not restore them when the existing node became visible. Numeric inspection confirmed an empty shadow render list. It has been removed; candidate captures made with that filter establish shape/material evidence only, not cast-shadow acceptance. Prior shadow-on/off captures were inconclusive, as the reviewer correctly recorded. New captures use the actual retained render list and preserve the old evidence. The low-sun Gas diagnostic now produces a non-identical shadow result but also exposes self-shadow artifacts; targeted planet/ring shadow acceptance remains open. No production shadow code was changed.

The planet authoring contract now explicitly covers curved normal transport, UV/material parity, glTF double-sided illumination, linear versus sRGB texture channels, recessed-floor rays and multi-seed/reference-scale checks. These supplement—not replace—the required worker/build-ahead/retention/disposal and real Flight/Map→Observe hardware validation.

### 2026-09-14 — corrected color export and remaining macro work

The independent Astra comparison of corrected Rocky8, Volcanic20, Ice20, Crystal6 and Temperate1 leaves all five gates open. Large smooth regions, missing intermediate geology and feature placement remain more important than further fine detail. New actual screenshots now include a populated shadow list; diagnostic normal bias0.006 is recorded separately from production settings. Captures remain SwiftShader appearance evidence, never hardware transition timing.

Several procedural source scripts wrote linear palette values directly into sRGB PNGs. Preserved successors encode those colors correctly without increasing the authored palette: Rocky9, Crystal6, Volcanic21 and Ice21. Toxic3 already encoded correctly; its saved PNG was checked rather than modified blindly. The isolated material adapter is also tested against Babylon's installed glTF adapter for alpha, clearcoat, normal-map handedness and double-sided lighting.

Rocky9 earns a bounded macro-composition subpass: coherent pitted terrain and readable mineral exposures replace blank surfaces. Rocky10 retains its placement while irregularizing selected pit mouths and the mineral fault. Volcanic21 and Ice21 improve finish but fail the main morphology gate; their successors target an integrated lava flow and clustered ice cliffs between stepped banks. Temperate2 addresses smaller unequal continents and forest clusters. Crystal7 reuses the denser native Rocky9 crust, lowers excessive bare outcrop walls and changes crystal distribution. These successors are still undergoing actual two-angle/reference-size review.

Toxic4 retains the corrected recessed floors and adds irregular basin edges; main-world review remains open. Fog3 closed shells and Fog4 nested shells fail the local appearance gate as visible membranes. Fog5 native alpha cards are a preserved diagnostic with card-junction/fringe limitations, not an accepted volumetric effect. No candidate has been published, and canonical records are being brought forward without implying owner sign-off. The separate19-moon coverage matrix is in `output/playwright/planet-reference-20260914/moon-coverage-plan.md`; first rocky-moon source exists but has not passed actual comparison.

### 2026-09-14 — additional working gates and rendered-source diagnosis

Root and independent Astra now agree on bounded main-world working passes for Rocky10, Temperate3 and Volcanic23, alongside Desert14, Ocean7 and Gas5. These are meaningful reference-gap closure in inspected seed38 views, not exact equivalence, moon coverage, owner approval or hardware acceptance. Volcanic23's body/flow is frozen; an isolated-viewer omission of its existing intended smoke was identified and is being restored using the existing builder/material, not a new smoke design.

Ice23 picking identifies the large blank left-center area as the ground placement rather than regional snowplains. Ice24 therefore adds native ground relief and preserves its authored radius through composition, including its height envelope in floor clearance. Whole-body visual review remains open. Crystal7/8 were rejected before complete export because corner-expanded JSON/GLB optical seams are not closed authoring topology. Crystal9 appends original editable Blender objects and passes full attribute audit. Composition10–12 trials address medium-cluster distribution; the central gap remains under diagnosis. One Crystal10 capture was interrupted by overlapping navigation and is explicitly excluded; a fresh retry is preserved, and the capture runner now rejects concurrent use with a lock.

Toxic5 broadens connected chemical terrain and the native fog5 clearance helper now tests final neighboring terrain before placement. Soft limb wisps are visible; the remaining art gap is intermediate cracked wet terrain joining the ponds. The first rocky moon actual capture retains deep bowls but fails the fractured-crust reference morphology; no moon pass is claimed.

Latest `npm run build` passes. Latest `npm run check` fails in the existing `packages/render/src/render-budget.test.ts` import of missing `@sidereal/sim/wayfarer-conversion-candidate`. No simulation/authority file was changed to work around it. Current isolated viewer TypeScript and the six Crystal9/Volcanic22 successor tests pass, as do their complete native attribute audits. Check/build logs are `check-latest.log` and `build-latest.log` in the evidence root. The checkout still has no Git remote, so the required PR cannot yet be created; the repository question remains unanswered.

### 2026-09-14 — Ice/Crystal working passes and isolated LOD recording

Root and independent Astra agree Ice25 meaningfully closes the authored morphology gap: unequal thick snow districts now expose connected blue ground, with deep cavities and grouped shafts. The paired Shadows Off diagnostic removes the remaining cap hatching; this is a separate shadow-rendering issue and is not justification for remodeling sound surfaces. Crystal13 also passes the bounded main-world gate:24 intermediate clusters selected against the existing large/medium formations close global coverage holes, with tip visibility tested against final overlapping terrain. Volcanic23 retains its pass after restoration of the existing intended smoke path.

Rocky moon candidate2 passes against exact rocky-moon-1 only. Six further solid-moon variants now have two close views and actual reference-size captures; individual reviews remain open. Toxic6 improves basin connections but retains overly quiet dark ground; the next bounded native revision targets that actual placement while freezing pools, chimneys and fog.

The isolated review now accepts validated direct evidence-kit paths and includes a camera approach/retreat recorder for frame intervals, pending/retained levels, enabled nodes and material identities. Candidate mode skips the hidden baseline environment's creation/update. Existing PBR parity tests and the two direct-path/NullEngine recorder tests pass after applying the proposed patch. The GPU shared preview still reports visible:false on the latest retry; no hardware measurement is claimed. All current captures are SwiftShader appearance/count evidence. This isolated recorder does not substitute for normal Flight and Map→Observe acceptance.

### 2026-09-14 — all main-world morphology gates; individual moon work continues

Toxic7 now passes the bounded two-agent main-world visual gate. Native medium cracked/terraced relief reaches the actual blank ground identified by camera picking, while Toxic6 pools/chimneys/materials and fog5 remain unchanged. All nine main-world candidates now have meaningful visual-gap closure at the inspected views; broader seeds, optical refinements, production integration and normal-game hardware acceptance remain open. Ocean7's current-optics triplet confirms its earlier pass survives the corrected material adapter and populated shadow list.

Individual moon working passes now cover Rocky1/2, Desert1/2, Gas2 and Ocean1. Temperate1/2 need larger connected green native cap districts; Ocean2 needs pale upper-district contrast. Ice1/2 need medium glacial coverage rather than smooth blue/white substrate. Volcanic1/2 need exposed narrow distributed faults checked against final overlapping terrain. Gas1/3 remain open: a hypothetical magenta parent light gives diffuse pink wash, and sparse non-emissive lower-roughness mineral r002 trials have not yet produced the localized optical accents of the exact crops. Native interpretation is explicitly distinguished from known reference illumination; no one-sided light is baked as factual albedo.

Canonical Ocean, Gas, Volcanic, Crystal, Toxic and Ice ledgers now preserve their intervening sources/export/capture history through Ocean7/Gas5/Volcanic23/Crystal13/Toxic6/Ice25. Subsequent Toxic7 and moon checkpoints follow separately. No owner final approval or production publication is claimed. The remote is now the owner-confirmed github.com/dastari/sidereal_spacetime; it is currently empty, and the owner has tasked another agent with creating the intended main baseline.

### 2026-09-14 — actual isolated RTX4080 evidence, timing gate still open

Using the owner's corrected host sidereal.tail7a58a6.ts.net:5173, the staged Ice25 candidate successfully loads on NVIDIA GeForce RTX4080 Laptop GPU. Exact selected kit/textures/HDR are staged under ignored review tooling via an explicit managed command; Vite's output/source deny rules are preserved. Saved evidence includes hardware-isolated-lod.json, hardware-isolated-retained-lod2.png and hardware-isolated-hero.png/.json under ice-r025. The first image deliberately captures retainedLOD2 at close camera before the next update; the hero image records activeLOD0 with default shadow normalBias .003. These are isolated canvas captures, not normal Flight/Map screenshots.

The54-second camera recording contains54 frames, all three active LODs, zero frames with no enabled planet geometry, zero simultaneous visible LOD nodes, and the same material-ID set throughout. However frame intervals reach1009.3ms even away from transitions, consistent with approximately1Hz preview delivery despite visible:true. Maximum measured update/render submission is21.4ms. This does not pass the20ms transition target and cannot attribute the preview's frame pacing to LOD work. LOD0/2 were already retained; LOD1 built during the recording. Production Flight/Map acceptance remains unmeasured.

Latest full build passes. Full check now reaches2046 tests (the earlier missing construction import is resolved externally), with2044 passing and two timeouts: existing construction-review-origin and planet-terrain stress cases during concurrent build/test work. No authority code was changed. The current focused11-file/54-test suite and applied viewer/worker TypeScript pass; Gas moon native material trials pass4 additional geometry/material/retention checks. A quiet rerun of the two timeouts is pending.

### 2026-09-14 — owner Ice feedback and repository baseline

The owner viewed the actual Ice25 preview and said: “The ice planet looks good, The only thing I would say is that the ice is that some of the tall ice shards should be like glass/ice transparent.” This is positive morphology feedback plus a specific material change request, not blanket final sign-off. Ice26 is scoped to a selected subset of tall shard side surfaces with native glass/ice PBR transmission, preserving opaque snow caps and existing geometry. Runtime optical metadata must match glTF transmission/volume, with one shared refraction resource and unchanged material identity across retained LODs.

The requested GitHub main baseline is now available. A separate worktree/branch art/planet-reference-alignment-20260914 has been created from origin/main (599d2c7a); unrelated construction work remains in its original checkout. Two prior full-suite timeout cases pass on a quiet single-worker retry (nine tests); full-build success is unchanged.

### 2026-09-14 — selected transmissive Ice26 and further moon passes

Ice26 changes only the material roles of selected tall-shard side triangles. Six named native source objects contribute7,632 optical triangles per composed body; total185,544 triangles, positions, indices, normals, UVs and all ten original maps remain unchanged from Ice25. Opaque caps and ground retain their original materials. Raw and final GLBs carry transmission .82/.68, IOR1.31 and native volume attenuation; five source tests, complete attribute/optical audits and source TypeScript pass. The isolated importer now reproduces those glTF values and shares one scene refraction target across the body's two glass materials and retained LODs. Three additional NullEngine optical/resource tests and applied TypeScript pass. This is a review candidate, not production integration.

Actual Ice26 glass-wired seed38 two-angle/reference-size captures now exist. Root observes selected transparent/refractive shafts, particularly near the lower-left limb in angle2; the existing shadow hatching on some opaque caps remains an independent technical issue. Native optical-preview.png provides a closer view of the selected shafts. The software renderer proves appearance/counts only. The public GPU tab loaded Ice26 and its materials but its document reports hidden with zero animation callbacks; no new hardware timing result is asserted.

Root and independent Astra agree on working passes for Temperate moons1/2 and Ocean moon2 r002, with larger connected green districts and pale upper-district contrast respectively. Earlier Toxic moons1/2 also pass individually. Eleven of19 moon crops now have bounded working passes. Ice-moon1 r002 captures closed three separate software browser sessions during preparation; failure logs are preserved and composition cost is under investigation. Ice/Volcanic r002 and Crystal r002 whole-body reviews remain pending, as do the two unresolved gas-moon optical accents. These statuses do not imply all-seed, LOD, owner-signoff or publication acceptance.

Ice26 follow-up: root and Astra agree on a bounded selective-glass working pass (`reviews/ice-r026-selected-glass.md`). The local oblique capture shows rear geometry through a selected foreground shaft; its second angle is terrain-occluded and is not useful glass evidence. A software1574×907 approach/retreat record retained all three levels with zero enabled-geometry blank/overlap frames and both glass materials referencing target28. Software intervals reached5686ms and are explicitly not hardware timing acceptance. The identical Ice geometry currently creates redundant retained copies; the lifecycle follow-up is evaluating explicit fixed-detail reuse rather than disguising those copies as useful LOD reduction. Five optical/resource tests now cover shared-material owners and borrowing a loader-owned target. Full `npm run check` and `npm run build` pass (`check-ice26.log`, `build-ice26.log`); no production asset was published.

### 2026-09-14 — bounded worker lifetime and complete moon history checkpoints

The isolated candidate now owns one worker per body, initializes it once with selected kit URLs, sends small build descriptors thereafter, rejects late/failed jobs, and removes unpublished upload nodes on disposal or compilation failure. Twelve applied lifecycle/transmission/path tests and applied TypeScript pass. Exact Ice25/26 revision/layout contracts use one fixed-detail node while requestedLOD remains available for shadow selection; unknown revisions retain normal ready-only caching. This is isolated tooling, not a replacement for the production shared priority scheduler. Initial main-thread JSON parsing and indivisible GPU upload submissions remain limitations.

Ice-moon compositor3 hoists invariant frames/stencils and uses bounded numeric floor indexing. Frozen2/optimized3 output is exact for both moons; all36 source/projected comparisons (two composers, three seeds, everyLOD) preserve buffers, ranges and materials. Explicit, hash-verified runtime projections omit only unused native LOD variants, keeping full source history and the64MiB staging cap. The first staged attempt exposed a Vite directory rewrite that removed a trailing slash; the failed JSON/timeout capture is preserved. Correcting URL joining gives successful actual browser triplets for both moons, with worker build times1855.9/1370ms in SwiftShader. No browser crash cause or hardware20ms pass is inferred. Independent review passes moon1; moon2 still needs a broader blue district through its large white center.

Nine canonical family moon checkpoints preserve35 source revisions, individual review states and exact main-world revision pins without changing older revisions, owner feedback, approvals or reference mappings. Gas draft coverage was corrected transparently from nonexistent planets--gas-giant-world to planets--ringed-gas-giant; the complete prior ledger/hash remains preserved. Scoped evidence/history checks pass. A full check had one terrain stress timeout (2047/2048 passed); the isolated five-test retry passes and build passes. Global art validation still reports source inventory changes and stale global generated views, which were not overwritten amid unrelated art work.

### 2026-09-14 — Ice moon blue cut and native moon review follow-up

Ice-moon2 r003 retains the proven three native variants/materials of r002 and adds one existing glacial gorge through the broad white center. The r004 compositor keeps the new placement identity and all authored attributes. Three tests pass, including a material-bearing NullEngine surface ray resolving the new blue-cut placement; an initial bounds-only ray test failure is preserved and corrected. All LOD buffers are identical and the explicit fixed-detail policy keeps one node. Root and Astra agree on the two-angle/reference-scale working pass. Volcanic moon1 r003 also passes after unequal exposed hot patches remain readable at82px. Fourteen of19 moons now have bounded working passes.

Volcanic moon2 still needs more readable localized hot area at47px. Crystal r004 native connected faceted bodies improve the family identity, but actual renders appear thinner/more open than unmatched native previews. This is an unresolved native/runtime parity question; do not change source thickness to conceal a possible import defect. Gas moon1/3 r005 use a documented bounded luminous-mineral interpretation on selected existing native facets; actual intended-glow review remains pending.

The public Ice26 preview has rendered4,813 frames on the RTX4080 Laptop GPU since the earlier zero-frame probe, with two transmission materials and one target. A fresh probe and reopen attempt at06:11UTC both report hidden and the same frame count. This supersedes the earlier claim that it never advanced, but provides no accepted timing or normal Flight/Map evidence.

Ice26 fixed-detail follow-up: the explicit staged Ice26 route at1574×907 records34 software frames across approach/retreat thresholds, zero missing-planet frames and zero overlapping visibleLOD nodes. OnlyLOD0 is retained; the same two glass material IDs15/16 share target28. This removes the earlier redundant identical-geometry copies without changing authored surfaces. Maximum software interval7240.7ms/render submission132.7ms are not hardware acceptance. Evidence: ice-r026/software-fixed-detail-glass-lod.json.

Clean-branch preparation initially linked the source node_modules because external dependency versions matched. Fullcheck exposed workspace-package resolution into the unrelated dirty source checkout, invalidating that setup for whole-project validation. The symlink was removed, npm ci installed isolated dependencies from the branch lockfile, and fullcheck is being rerun. Original failure log is preserved; no unrelated package source is copied to silence it.

Gas moon1/3 r005 both pass root and independent Astra review at corrected reference sizes. The first intended-glow captures had the moons' apparent reference sizes reversed (~82/~50px); those remain labeled diagnostic evidence. The corrected-scale triplets use ~50px for moon1 and~82px for moon3, matching their exact50×53 and83×83 crops. Localized native luminous mineral patches survive both sizes while preserving the lilac cratered foundation. The reference's warmer highlight core and brighter rim remain finish differences. This is an explicit art interpretation, not a claim about the illustration's physical illumination. Sixteen moons have working passes.

Isolated branch fullcheck fails on two absent baseline modules imported by unchanged tracked code: packages/render/src/debug-collision-geometry.ts and scene-material-registration.ts. Neither exists in origin/main599d2c7a. The first isolated build lacked the pinned local SpacetimeDB executable; a tool-directory link reuses the installed executable without changing services, and build is retried. The planet-only test closure is being rerun with isolated node_modules; no package source was changed to mask baseline failures.

### 2026-09-14 — OOM recovery and bounded validation

The owner reported a likely OOM. On recovery, cgroup memory.events reports three cumulative OOM kills; the prior reference-check process is absent and its log has no final result. Kernel logs are inaccessible, so victim/timestamp attribution is not claimed. The reference check now uses a separate4GiB-heap process per test file with only one heavy task at a time. This prevents cross-file parsed-kit retention and preserves individual completion results. Native revision5 generation had not completed; source4 and matched-camera topology diagnostics survived.

Crystal's native topology audit found coherent-volume orientation differs from636/1356 faces on moon1 and200/424 on moon2 despite normals matching individual triangle winding. Blender's two-sided shading concealed faces culled by opaque runtime rendering. The bounded successor will correct orientation while preserving exact triangle surfaces, UVs and materials; no thickness or culling workaround. Gas1/3 working passes remain16/19 total. Volcanic moon2 geometry is sufficient afterr004; only an immediate localized warm material shoulder remains under review.

The isolated branch build succeeds through world and client, then fails dashboard on the missing baseline scene-material-registration import. Fullcheck also reports missing debug-collision-geometry. Both are absent from origin/main and outside this art change. Earlier successful fullcheck/build in the shared checkout therefore do not establish a clean baseline PR build.

### 2026-09-14 — repaired Crystal volume, failed finish trials and resolved Volcanic bank identity

Crystal moons1/2r005 repair636/200 native polygons while retaining86/26 panel components and the exact original exported triangle surfaces, UVs, materials/maps and signed normals. Seven exact-normal/frontface/retention tests and GLB parity pass. Native polygon reevaluation chooses115/43 different triangles after reversal; the explicit validated export cache preserves the original authored diagonal inventory, and source/cache correspondence is recorded. Actual staged browser triplets confirm closed panels. Root/Astra keep the full art gates open: broad smooth panel districts still lack selected connected fracture hierarchy. Native r006 adds preserved exact base geometry plus terraces but fails author and Astra native review as repeated pads/towers; it is preserved without runtime integration. A bounded native replacement of selected cap districts is in progress.

Volcanic moon2r005 preserves all geometry and adds native warm material to139 adjacent B-edge triangles. All12 GLB audits and two tests pass, but actual47px review fails because only the bottom/upper-left shoulder changes. Actual material-bearing browser picks resolve right/lower hot banks to crust-4 and upper rim to crust-0, both authored A regions; the earlier warm bottom resolves B-edge crust-9. Evidence bank-placement-probe.json and bank-diagnostic captures now directs a local A-bank material successor. This is no longer an unverified color adjustment to unrelated surfaces.

Canonical checkpoints Ice28,Gas7,Vol25,Crystal15 preserve late source/failed-attempt histories and all current OPEN verdicts, keeping main pins26/5/23/13 and every previous approval/mapping unchanged. The art branch initial snapshot is commit4392ead0; LFS upload is in progress. Newer r005/r006 evidence is a subsequent scoped update. Thirty-four scoped files/187 tests and18 Python tests have passed in isolated/bounded processes; shared OOM count remains unchanged at3. Whole-branch check/build still expose the two baseline missing modules described above.

A fresh post-OOM shared preview reports a GeForceGT730, not the baselineRTX4080. It reaches27 frames with the Ice26 worker complete but one pending upload, then the repeated probe remains at27 frames while tool visibility is false (document visibility says visible). No settled hardware capture or timing acceptance is asserted; evidence ice-r026/hardware-recovery-probe.json. Continue using preserved native/software visual evidence while the preview delivery limitation remains.

### 2026-09-14 — Volcanic moon2 working visual pass

Volcanic moon2r006 warms455 existing A-bank triangles around three native hot objects resolved from actual browser hits. Geometry, normals, UVs, all seven material definitions and r005 B-edge remain exact; twelve GLB variant audits and two focused tests pass. Actual two-angle/47px captures now show separated red-shoulder/orange-core sectors at top, right and lower limb. Root and independent Astra agree the speck-only gap is meaningfully closed. Flat red close patches and the reference's brighter/softer rim are remaining finish differences. Seventeen of19 moon crops have working passes. No hardware, broader-seed, final owner-approval or publication acceptance is inferred.

### 2026-09-14 — Crystal r009 actual review after memory recovery

Both r009 moons pass the root and independent Astra morphology comparison at two actual browser angles and true reference-size views (79/52px). The finite terminal cuts retain the successful interior fracture geometry while eliminating the unsplit outer interval; nine source/geometry/material/LOD tests pass. Actual workers settle with one retained fixed-detail node and zero pending builds. Screenshots and JSON are preserved under crystal-moon-{1,2}-r009/terminal-cut-seed38*. These SwiftShader captures establish appearance and counts only.

The full visual gate remains OPEN: both distant views lack the references' distinct bright pink-white mineral shoulders. A bounded r010 native material-assignment iteration is authorized within the existing owner request; geometry and whole-body lighting remain fixed. Seventeen of19 moon visual gates remain passed. Heavy Blender, browser capture and test jobs are serialized; cgroup oom_kill remains3. Draft PR2 exists at https://github.com/Dastari/sidereal_spacetime/pull/2; newer evidence follows its initial4392ead0 snapshot in a scoped subsequent commit.

### 2026-09-14 — Crystal r010 closes the remaining two working visual gates

Root and independent Astra inspected both actual r010 angles and79/52px views against both exact moon crops. The selected bright mineral shoulders now remain legible at distance while the connected stepped purple bodies retain their scale difference and irregular rims. Both working visual gates pass: all19 individual moon crops now have explicit root+Astra assessments, alongside nine main-world working art/morphology passes. Fine fracture density, broader anchor facets and sparser rim luminosity remain finish differences; these are not claims of reference equivalence or defect-free rendering. Exact reports: reviews/crystal-moons-r010-{root,runtime}.md and visual-scope-consistency-20260914.md.

Native material-only10 changes27/15 triangles on10/6 actual-screen-traced polygons using an existing pale-pink role. All geometry, normals, UVs, indices, material definitions and maps remain identical to9. Nine focused source/NullEngine tests and full GLB/kit audits pass. Both runtime workers settle with one fixed-detail retained node and zero pending builds; exact revision qualification prevents threshold rebuilds. No hardware timing, all-seed visual acceptance, normal-game publication or final owner approval is inferred. Final isolated39-file/216-test closure and18Python tests pass. Required full-project check/build repeat the same unchanged upstream missing-module failures; logs are preserved in pr-preparation/final-full-{check,build}.log.

### 2026-09-14 — Owner continued remaining planet work; shadow diagnostics on RTX hardware

The owner requested remaining planetary work and another Astra comparison, stopping visual iteration when both agree the gap is close. All previous nine-main/19-moon working art passes remain intact. Fresh Ice26 screenshots reproduce cap hatching on both SwiftShader and RTX4080Laptop; no source-art repair is justified by the closed/outward native geometry audit. Normal-offset, back-face and unfiltered diagnostics are preserved under shadow-followup/. A slope-aware raster depth offset of1, units1 applied only during the shadow pass removes broad cap hatching in two hardware views while retaining contact/cavity shadows and selective glass; root and Astra grant a bounded technical visual pass. This uses the same1024 shadow map and native PBR surfaces. A resource-state restoration NullEngine test passes.

Shadow selection also needed semantic cast/receive metadata for smoke/core instead of names, active-descendant filtering across retained/pending LODs, and correct restoration of prior light exclusions/receiving state. Three focused tests pass after replacing decorated-Babylon-array deep equality with identity comparisons. Root is checking ring coverage/transparent casting next: existing2.7R shadow frustum is narrower than3.76R ring diameter, and blended dust does not cast by default. A temporary grey-receiver hardware diagnostic proves ring-shaped alpha shadows when enabled; it is not a new material proposal.

Fresh isolated Ice26 hardware routes at1574x907 preserve one visible retainedLOD and seven material IDs with no missing/overlap frames. Initial run1564frames includes65.1ms resize spike; warmed run1571frames includes22.3ms maximum interval and1.3ms maximum submission. Both remain below strict acceptance quality because20ms was exceeded; neither is normal Flight/Map→Observe acceptance. Entire frame arrays were retrieved in bounded80-frame chunks and saved, including failed results. The pane later stopped animation callbacks at gas frame22418 although manual still rendering works; do not represent manual stills as frame-loop timing.

### 2026-09-14 — R14/R15 shadow and complete-output LOD qualification

The integrated Ice26 capture initially regressed when conservative AABB spheres widened the shadow volume. Tight origin-centred vertex bounds are now computed once in the reference worker, with no frame-loop vertex scan; the shadow pass uses slope factor2/units2 and restores engine state. Root and independent Astra pass the resulting two-angle Ice26 stills and whole-ring Gas5 framing. Broad ice-cap interference is sufficiently suppressed; fine edge fringes and alpha stipple remain documented. No more art/still polishing is requested at these views. Gas alpha casting is supported by the preserved grey-receiver/restored-material hardware diagnostics; current low-sun texture-rich stills alone do not isolate that shadow conclusively.

Five NullEngine shadow tests and one worker-envelope test pass. Fourteen additional exact revisions pass complete actual-worker equality across seeds38/117/904 and LOD0/1/2, including fog and shadow bounds (15 tests total), permitting one retained fixed-detail node without redundant transition rebuilds. Capture tooling now waits for three advancing ready frames at each requested view with zero pending work; two deterministic harness tests pass. The56-case broader-seed visual run is beginning; these code checks are not visual all-seed acceptance.

Required npm run check and npm run build were rerun and still fail on unchanged upstream missing debug-collision-geometry and scene-material-registration modules; logs are pr-preparation/shadow-tight-full-*.log. Files changed: planet-shadows and its coverage/depth helpers/tests, semantic smoke/core metadata, reference worker/candidate bounds, fixed-detail qualification and capture settlement tooling. No authority, authored surface or material replacement is included.

No new settled normal-game Deck/Flight/scale2/Glow–Shadows–Lighting counters are available. The isolated RTX4080Laptop route records zero blank/overlap frames and stable material identity, but a warmed22.3ms interval exceeds20ms; later animation stalled. Final integrated stills use SwiftShader, establishing appearance only. R14/R15 hardware acceptance and normal-game activation remain open. Evidence is under output/playwright/planet-reference-20260914/shadow-followup/ and reviews/shadow-tight-ice26-gas5-review.md.


### 2026-09-14 — Cold shadow/material preparation, isolated hardware pass

The first near/far shadow switch compiled different PBR light orders; Ice transmission also renders opaque surfaces with the image-processing post-process define in its refraction target. Preparing only the ordinary visible material left that render-pass variant cold. The new serialized planet-shadow preparation adapter compiles initial far, hero and restored far orders using disabled preparation meshes and isolated lights, then the opaque-refraction variant with a private PBR configuration and shared source textures. It preserves live source materials, placement metadata, light state and body ownership, yields between stages, and disposes preparation resources on completion/failure. The manager API is reusable; this follow-up is currently exercised by the isolated review harness with prepareShadows=1, not wired into normal-game native loading.

Nine focused NullEngine tests cover ordered light defines, resource/texture identity, cancellation, allocation failure and manager serialization. The independent integration audit additionally reproduced and fixed a private material leak when a PBR plugin throws partway through cloning; the regression test passed after the ownership cleanup fix. A further camera test validates a cold far-first starting radius. The capture harness records source hashes at process start and retains its three advancing ready-frame gate. A real missing Vite optimized dependency caused one Desert moon capture to fail readiness; the original failure and console504 are retained. A fresh document succeeded. The mistakenly sized first retry is excluded from reference-scale judgment; retry2 uses the manifest's exact radius.

At1574x907 on the RTX4080Laptop, the completed Ice26 prepared-refraction route records1574 frames, maximum14ms interval/1.2ms submission, zero compiler events and no blank/overlapping levels. A separate fresh far-first Ice approach records1574 frames,11.2ms/4.3ms, one short compiler pair, and no blank/overlap. Gas5's fresh far-first route records1571 frames,17.6ms/3.6ms, two short compiler pairs, and no blank/overlap. These pass the isolated transition budget; they do not establish seated Flight, Map→Observe, F3 or whole-scene performance. Full frame arrays and earlier failing attempts are preserved under shadow-followup/. Root and independent Astra also pass the final actual-callback Ice26 hardware pair, retaining selective glass and native surface appearance.

The broader seed review is ongoing: all nine main candidates pass seeds117/904, while moon captures/reviews are proceeding serially. No additional art polishing is warranted for passed candidates. Normal-game activation, missing authored identities for the19 moon variants, and three oversized source payloads remain separate implementation gates documented in handoffs/planet_native_integration_readiness_20260914.md. Inactive modern schema validation adds five bounded tests but does not activate or fully validate those source geometries.

Required check/build after shader preparation again fail on the unchanged upstream missing debug-collision-geometry and scene-material-registration imports (check2/build1). Client and world build stages pass; dashboard bundling fails on the missing module. Exact logs: pr-preparation/prepared-shaders-full-{check,build}.log. Focused preparation/schema tests14/14, camera test1/1 and strict reference typecheck pass. No normal-game counters are claimed.


### 2026-09-15 — R14 reference visual iteration closed at the agreed working passes

Root and independent Astra agree that the defining reference gaps are meaningfully closed for all nine main planet families and nineteen distinct moon variants. The additional seeds117/904 now have56 completed jobs and168 actual close/alternate/reference-scale images, all inspected by an independent Astra reviewer against exact crops and accepted seed38 views. Every prior working pass survives; no further art polishing is requested. Root spot-checks and the earlier individual reference comparisons agree. This is the owner's requested stopping point for visual iteration, not a claim of pixel equivalence or final owner sign-off.

The evidence audit verifies all168 image hashes, exact source kit hashes/reference radii, and three advancing ready frames with zero pending builds for each view. The seed capture manifest initially omitted Ice moon1's accepted staged transport; both failed full-source attempts are retained, and retry2/seed904 use the existing unchanged source-to-runtime projection. Desert moon2's mistakenly sized first retry remains excluded; its corrected retry2 is the accepted reference-scale record. Browser-closed/504 failures are preserved without treating them as visual or hardware results. OOM kill counter remains3.

Twenty-two reviewed implementations now share a renderer-owned location with review re-export shims: nine main composers, five geometry/bounds helpers, five weather/smoke helpers and three material/transmission/local-light helpers. An automated source comparison verifies no implementation change except relative import paths. This prepares integration without duplicating or redesigning the accepted art. Normal-game loading/selection and the production shared worker remain unchanged.

Normal-game seated Flight/Map→Observe acceptance and settled Deck/Flight F3 isolation measurements remain unmeasured for these revised assets. The separate RTX4080 isolated shader-prepared routes pass20ms (Ice near-first14ms; Ice cold far-first11.2ms; Gas cold far-first17.6ms) with no missing/overlapping body; these do not close those normal-game gates. R14/R15 remain implemented review candidates with normal-game integration/measurement outstanding. Missing moon authored identities, validated payload packaging and explicit shared-worker/upload integration are documented in the integration handoff. No content/world/sim changes or heuristic moon mapping were made.

Exact reports: reviews/broader-main-seeds-review.md, broader-moon-seeds-first-nine.md, broader-moon-seeds-last-ten.md, broader-seed-completeness-audit.json and broader-seed-final-records.json. Evidence remains under output/playwright/planet-reference-20260914/; the isolated hardware records/screenshots are also preserved under output/playwright/render-plan/R15/planet-reference-followup-20260914/.

Final extraction validation: strict reference typecheck,42 TypeScript test files/233tests and20 Python tests pass, using one bounded process at a time. The automated22-file source identity audit also passes. Required npm run check/build again return2/1 on the same missing upstream renderer imports; world/client stages pass and dashboard fails. The separate documentation link check fails on existing absent referenced files, including docs/visual_theme.md, which is also absent from origin/main. Exact logs and counts are pr-preparation/extracted-final-*.log, extracted-final-validation.json and extracted-reference-counts.json. No full-project green status is claimed.

A fresh RTX4080 public review load after the shared-code extraction reaches an advancing ready scene with one retained Ice26 node, zero pending builds, two transmissive materials and one refraction target. Actual-callback screenshot/JSON: ice-r026/extracted-hardware-continuity.{png,json}. Root confirms retained appearance; this is a load/visual check, not another timing run.


### 2026-09-15 — Reviewed planets activated in Genesis

Owner requested that dashboard5174 `/planets` show the merged native planet work. Genesis now defaults to explicit selection of all nine main planets and nineteen moon variants; the original procedural editor remains available separately. Exact Blender geometry, authored PBR textures/optical properties, seed composition and fixed-detail qualifications are preserved. One shared worker/scheduler registers SHA256-validated bounded payloads, generates compact buffers and releases leased assets. Body materials/refraction are shared across retained LODs; preparation includes shadow/opaque-refraction variants and atmosphere. Selection publishes only from the render loop after readiness, keeps the old body until replacement and rejects stale completions. No authority, simulation, content, cutaway or F3 semantics changed.

Larger Desert/Toxic kits now use exact binary64/u32 transport below the unchanged64MiB limit; Ice moon1 uses its already-proven used-variant projection. All28 actual packaged registrations and full-detail compositions pass, maximum measured child RSS350.6MiB. Focused validation:13 files,31 passing tests and2 explicitly optional archived-source comparisons skipped (both comparisons passed during packaging); three of those tests are cross-selection NullEngine cases. Preview-specific TypeScript check passes. Required `npm run check` stops on preexisting absent debug-collision-geometry/scene-material-registration imports. Required full build stops on the isolated checkout's unavailable Spacetime binary; direct dashboard build reaches the same preexisting missing scene-material-registration module. These failures are recorded, not waived or called green.

Actual Genesis Ice26 canvas is captured on NVIDIA GeForce GT730 (1669x1132 render canvas); it reports activeLOD0, zero pending builds, two transmissive materials and one refraction target. Root and independent Astra agree its snow caps/craters, blue cliffs and glass/rim highlights preserve accepted Ice26 appearance. Owner also reports it looks good. This GPU differs from the RTX4080 baseline and the preview intermittently stops advancing when hidden: no20ms transition acceptance is claimed. Local SwiftShader captures prove appearance/counts only. Revised-asset seated Flight/Map→Observe and Deck/Flight F3 counters remain unmeasured; Genesis is not the game scene.

Changed areas: dashboard PlanetStudio/ReviewedPlanetStudio, renderer reviewed-native catalog/encoding/composers/materials/runtime/selection and shared worker, reproducible packaging/smoke scripts, exact public/reviewed-planets bundles, specification/ADR/architecture/changelog/handoff. Evidence: `output/playwright/render-plan/genesis-native/`. Dashboard/render version0.2.1. New PR delivery is tracked in the integration handoff. No further art iteration was performed.
