# Native catalog browser acceptance

2026-09-09 local date. Actual independently served dashboard at `http://sidereal.tail7a58a6.ts.net:5174/shipyard`, fresh disposable browser profile. No existing author draft, production identity or live world was modified.

The new Objects palette loads73 native cargo entries plus2 retained legacy cargo entries. All73 native labels were present in the palette. An actual Standard Small palette click followed by floorplan click created fitting UUID `86c8faf9-f2c8-414a-ab73-9b31f140a0ef` referencing asset `part-c06e4f5f6f6dace38e41` and its native visual hash. Save local draft and a full reload retained the same document UUID, fitting record/hash, all original tile topology and one undo-history entry. The source profile/draft namespace is local to this test. Evidence: `output/playwright/dashboard-cargo-final-draft.png`, visually inspected.

The new editor explicitly labels these as visual references and stores a footprint with `container:null`; this is not functional storage installation. Its Objects/Rooms palette filters equipment/cargo, so native floor artwork is not a selectable topology tile. At `/shipyard?assembly=legacy`, the actual floor category displays all16 native floor catalog assets along with32 retained legacy references. The floor palette thumbnails were visible in `output/playwright/dashboard-native-floor-catalog.png`, visually inspected. This verifies catalog access; it does not claim a native floor was placed in the new topology document. The legacy 3D preview was held during this catalog-only check, so the blank preview/loading text in that screenshot is not a 3D-render acceptance result.

Browser HTTP requests used exact-response pass-through from the running dashboard server to avoid Chromium local-network quirks. No asset/API response was replaced with authored test content. Development reload messages were held. The dashboard browser was closed before the next GPU reviewer. Native catalog access and local draft persistence do not imply blueprint publication, live refit or OIDC production authentication.
