# Genesis reviewed native planets

Date: 2026-09-15. Status: implementing owner-authorized Genesis activation.

Genesis /planets on dashboard5174 must render the nine reviewed main types and nineteen explicit moon variants from merged PR2. This is editor integration; it does not assign reference variants to authoritative world bodies. Preserve existing recipe import/export and procedural authoring in an explicit procedural mode. Default to reviewed native assets; expose native-supported selection, seed, orbit/zoom and gallery controls without suggesting unsupported terrain/palette sliders edit Blender surfaces.

Use renderer-owned immutable catalog descriptors with exact revision, style, source/runtime hashes, texture paths, weather, glow/local-light and fixed-detail policy. Stage validated native runtime bundles under the dashboard's assets/planets path with source provenance. Retain source archives. The64MiB serialized payload bound remains; lossless transport encoding may use typed numeric buffers with documented round-trip equality, or validated used-variant projection with complete-output equivalence. Never decimate, regenerate surfaces or raise the cap to hide an import failure.

Extend the existing planet worker/client with explicit reviewed-register/build/release operations, one worker and shared nextFrame scheduler per preview. Assets are fetched, hash-checked, parsed/validated and composed in the worker; return only header/material metadata during registration and compact buffer results for builds. No full kit parse/clone per LOD on the main thread. Distinguish registered asset identity from body/placement identity. Reject unknown layouts/revisions, failed registrations and stale/disposed results; terminate/recreate the worker on fatal failure and invalidate registration cache. Bound live asset registrations through leases; gallery preparation is serial.

The native upload adapter uses the existing LOD cache: disabled unpublished levels, frame yields between uploads/compiles, pre-threshold preparation, synchronous ready-only swap and retained levels. Share PBR/textures/refraction across body LODs. Prepare actual shadow light orders and opaque-refraction variants. Every created mesh has role and body-resolvable triangle placement ranges. Preserve weather/smoke, authored UV/normals and material channels. Use existing shared shadow/glow managers; no name selectors or generic replacement materials.

Acceptance: all28 catalog entries selectable, actual loaded revision visible, seed changes regenerate, gallery and orbit/zoom work; stale completion cannot replace newer selection; error path is visible and retryable; unmount releases resources. NullEngine tests cover identity, swap/retention, materials and disposal; worker tests cover registration/build/error/retry/leases and complete output parity. Run check/build, real Genesis browser review with screenshots/JSON, and hardware transition sampling. Report missing full-game Flight/Map evidence separately. Deliver through a new PR; do not merge without instruction. Source preview5174 activation is authorized by the owner's request. Never modify world/sim/content or unrelated Shipyard files.
