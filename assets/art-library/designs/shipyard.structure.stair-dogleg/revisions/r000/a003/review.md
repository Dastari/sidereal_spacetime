# Native dogleg r000/a003 — bounded geometry qualification

The complete native dogleg passes all55 geometry checks. The editable Blender source retains207 authored objects; the visual GLB preserves their evaluated surfaces in eight semantic groups,22 material primitives,37,860 triangles and six opaque single-sided PBR materials. No texture, rig, proxy replacement or runtime installation is supplied. All source, rendered evidence, failed attempts and hashes are retained.

The kit retains the proposed4×6m reservation and seventeen0.1875m rises, with0.3125m goings. Nine rises lead to the supported intermediate landing atZ1.875m; eight return rises meet the actual native upper floor atZ3.375m. Measured flight width including rail shoes is at least1.5999999m. Closed native underflight panels prevent the visible stairs from implying a clear service walk underneath. This is not pressure or structural-load qualification.

The independent validation matches every exported triangle to the actual evaluated authored triangle multiset within1µm, with unchanged source materials. Eighteen contact patches are independently covered by planar top triangles of the actual native primary tread, pan or floor surface. The native collision triangle archive is separate from the visual export;259 containing object bounds feed the conservative capsule audit.

Seventeen step interfaces certify the full radius0.3m, height1.8m capsule over every point in a0.8m-wide,15mm-deep downhill-edge launch strip. Each permitted step lifts by0.1875m, advances0.3125m, and settles on the actual adjacent support. The exact reversed trajectory uses the same swept volume. Each phase's complete capsule union is bounded by its swept capsule-core AABB expanded by radius; distance to every containing native solid AABB is checked. Five continuous approach/turn corridors also pass. The lower roof and upper slab are included in every check, rather than hidden for authority.

All15 tread-center resting alternatives fail. Those forbidden stops are retained in `rejected-center-stops.json`. The valid interfaces require a bounded step controller that stops on a verified stance strip or reverses the unfinished bounded step to its supported source. There is no permanent floating ramp, reduced capsule or assertion that arbitrary tread coordinates can settle. The static geometry qualification is distinct from the remaining server control, interruption/reconnect, occupancy and browser work.

The fixture uses an8×10m native board. All eight kit groups are placed at board[2,2,0];20 lower-floor,16 upper-floor and16 lower-roof panels make60 total source placements. The four omitted upper-floor/lower-roof origins are board(2,4),(4,4),(2,6),(4,6), giving a real4×4m panel opening. Fascia/guard intrusions remain actual independently checked solids, not a claim that the entire nominal opening is empty. All audit coordinates are in board XYZ; editable kit meshes remain kit-local XYZ.

Actual cutout, lower cutaway, full native slab context and top renders were inspected. a001's riser collision and a002's duplicate coplanar joint faces are corrected in a003. The upper native floor edge now supplies its physical final riser; the intermediate pan supplies its own front edge, with a small authored closure only beneath it. `capsule-fit.png` shows four independent rest/turn review placements together, using a presentation cutaway; it is not a multiplayer or animated character demonstration.

Delivery status is `native-geometry-qualified` for this fixed fixture and exact declared capsule/support envelopes. Runtime publication remains false. Owner final artistic sign-off remains null. Higher ceilings, differently sized/equipped actors, arbitrary mirrored/remixed layouts, armor pose fit, dynamic damage and gameplay integration require their own checks. The existing ladder source and runtime are unchanged.
